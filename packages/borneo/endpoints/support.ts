import { createBorneoEndpoint } from './factory';

export const postSupportChatQuery = createBorneoEndpoint(
	'postSupportChatQuery',
	'BORNEO_POST_SUPPORT_CHAT_QUERY',
	'borneo.support.postSupportChatQuery',
);
