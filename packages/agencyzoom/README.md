# @corsair-dev/agencyzoom

AgencyZoom plugin for Corsair.

## Install

```bash
pnpm add @corsair-dev/agencyzoom
```

## Endpoints

| Operation | Operation ID | Risk | Description |
|-----------|--------------|------|-------------|
| `auth.logTheUserIn` | `agencyzoom.api.auth.logTheUserIn` | `write` | Authenticates an existing AgencyZoom user using their email (as username) and password to obtain a JWT for API access; this action does not support new user creation. |
| `auth.logTheUserOut` | `agencyzoom.api.auth.logTheUserOut` | `write` | Use this action to log the current user out of AgencyZoom by invalidating their active session token. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `auth.v4SsoLogTheUserIn` | `agencyzoom.api.auth.v4SsoLogTheUserIn` | `write` | Logs a user into the AgencyZoom platform via Single Sign-On (SSO). Returns JWT on success or error details on authentication failure. |
| `contact.batchCreateContact` | `agencyzoom.api.contact.batchCreateContact` | `write` | Creates 1 to 5 new contacts in AgencyZoom in a single batch API call. |
| `customers.createACustomerNote` | `agencyzoom.api.customers.createACustomerNote` | `write` | Adds a new note to an existing customer's profile using their ID; cannot be used to edit or retrieve existing notes. |
| `customers.deleteACustomer` | `agencyzoom.api.customers.deleteACustomer` | `destructive` | Irreversibly deletes a customer and all associated data in AgencyZoom using their `customerId`; useful for offboarding or data privacy compliance. |
| `customers.deleteACustomerFile` | `agencyzoom.api.customers.deleteACustomerFile` | `destructive` | Permanently deletes a specific file, identified by `fileId`, associated with a customer, identified by `customerId`. |
| `customers.deleteACustomerPolicy` | `agencyzoom.api.customers.deleteACustomerPolicy` | `destructive` | Permanently deletes a specific policy associated with a customer, for instance, if it's cancelled or inactive; this action is irreversible and requires caution. |
| `customers.getAmsPoliciesForACustomer` | `agencyzoom.api.customers.getAmsPoliciesForACustomer` | `read` | Retrieves a customer's synchronized Agency Management System (AMS) policy data (typically a single policy); requires an active AMS integration, may return empty/default values if data is missing. |
| `customers.getPoliciesForACustomer` | `agencyzoom.api.customers.getPoliciesForACustomer` | `read` | Retrieves from AgencyZoom all insurance policies for an existing customer (identified by `customerId`), including policy details like type, carrier, premium, effective dates, and assigned agents. Excludes premium payment history or claims information. |
| `customers.getTheCustomerDetails` | `agencyzoom.api.customers.getTheCustomerDetails` | `read` | Fetches comprehensive details for a specific customer, including personal information, policies, notes, tasks, files, and custom fields, using their unique customer ID. |
| `customers.getTheCustomerTasks` | `agencyzoom.api.customers.getTheCustomerTasks` | `read` | Fetches all tasks (read-only task data) for a customer by `customerId` to review their activities, follow-ups, and action items; the `customerId` must be valid. |
| `customers.searchCustomers` | `agencyzoom.api.customers.searchCustomers` | `read` | Searches for customers in AgencyZoom using criteria like contact information, policy details, or custom fields, with options for filtering, sorting, and pagination. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `customers.updateCustomer` | `agencyzoom.api.customers.updateCustomer` | `write` | Updates an existing customer's information in AgencyZoom using their unique customerId. |
| `emailThreads.deleteMessage` | `agencyzoom.api.emailThreads.deleteMessage` | `destructive` | Deletes a specific message from an email thread within AgencyZoom. Requires the message ID to identify which message to remove. Returns the updated thread information with the remaining messages after deletion. |
| `emailThreads.deleteThread` | `agencyzoom.api.emailThreads.deleteThread` | `destructive` | Permanently deletes a specific email thread from the AgencyZoom system, identified by its `threadId` (expected by the endpoint), provided the thread exists. |
| `emailThreads.getThreadDetails` | `agencyzoom.api.emailThreads.getThreadDetails` | `read` | Searches and retrieves detailed information for email threads in AgencyZoom; no explicit search criteria are passed in this request. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `emailThreads.markThreadAsUnreadApiEndpoint` | `agencyzoom.api.emailThreads.markThreadAsUnreadApiEndpoint` | `write` | Marks an email thread in AgencyZoom as read or unread using its `threadId`; this action does not modify message content and the specified `threadId` must refer to an existing thread. |
| `emailThreads.searchEmailThreads` | `agencyzoom.api.emailThreads.searchEmailThreads` | `read` | Retrieves a list of email thread metadata from AgencyZoom, suitable for an overview when no specific filtering, sorting, or pagination is needed, as results are subject to default server-side limits and ordering. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `leads.batchCreateLead` | `agencyzoom.api.leads.batchCreateLead` | `write` | Creates up to 5 new leads in AgencyZoom; all referenced entity IDs (e.g., pipeline, user, custom field names) must exist, and organization `name` is required if `isBusiness` is true. |
| `leads.changeStatusForLead` | `agencyzoom.api.leads.changeStatusForLead` | `write` | Updates a lead's lifecycle status (e.g., Active, Won, Lost, X-Dated) and can optionally manage its workflow, pipeline, stage, source, or associated tags. |
| `leads.createALeadNote` | `agencyzoom.api.leads.createALeadNote` | `write` | Adds a new note to an existing lead in AgencyZoom, identified by `leadId`. |
| `leads.createALeadOpportunity` | `agencyzoom.api.leads.createALeadOpportunity` | `write` | Adds a new sales opportunity to an existing lead in AgencyZoom, requiring a valid `leadId`, `carrierId`, `productLineId`, and that custom field names match existing definitions in AgencyZoom. |
| `leads.createALeadQuote` | `agencyzoom.api.leads.createALeadQuote` | `write` | Creates a new insurance quote for an existing lead in AgencyZoom, using valid carrier and product line IDs, to track a proposal; this action does not bind policies or process payments. |
| `leads.createBizLead` | `agencyzoom.api.leads.createBizLead` | `write` | Creates or updates a business lead in AgencyZoom using detailed personal and company information, for B2B sales and marketing lead management. |
| `leads.createLead` | `agencyzoom.api.leads.createLead` | `write` | Creates a new lead or updates an existing one in AgencyZoom; ensure `pipelineId`, `stageId`, `leadSourceId`, and `assignTo` reference valid, existing entities. |
| `leads.deleteALeadFile` | `agencyzoom.api.leads.deleteALeadFile` | `destructive` | Deletes a specific file (identified by `fileId`) associated with an existing lead (identified by `leadId`); this operation is irreversible. |
| `leads.deleteALeadOpportunity` | `agencyzoom.api.leads.deleteALeadOpportunity` | `destructive` | Permanently deletes an existing opportunity (by `opportunityId`) associated with an existing lead (by `leadId`) when it's irrelevant, closed, or erroneous; the lead itself remains. |
| `leads.deleteALeadQuote` | `agencyzoom.api.leads.deleteALeadQuote` | `destructive` | Deletes a specific quote from a lead, requiring that the lead and quote exist and are associated. |
| `leads.getLeadFiles` | `agencyzoom.api.leads.getLeadFiles` | `read` | Retrieves metadata (id, title, MIME type, size, dates, creator info) for files attached to leads in AgencyZoom. Optionally filter by `leadId` to get files for a specific lead, or omit to retrieve files across all leads. Returns file metadata only, not the actual file content. |
| `leads.getLeadNotes` | `agencyzoom.api.leads.getLeadNotes` | `read` | Fetches the complete history of notes for a specific lead by `leadId` (which must exist), useful for reviewing context for communications or follow-ups; this is a read-only operation. |
| `leads.getLeadQuotes` | `agencyzoom.api.leads.getLeadQuotes` | `read` | Retrieves all insurance quotes (active and inactive) for a specific lead ID, useful for reviewing or tracking quote history. |
| `leads.getLeadTasks` | `agencyzoom.api.leads.getLeadTasks` | `read` | Retrieves all tasks for a specific lead, identified by its `leadId`, to review its activity history or manage follow-ups. |
| `leads.getTheLeadDetails` | `agencyzoom.api.leads.getTheLeadDetails` | `read` | Retrieves comprehensive details for a specific lead in AgencyZoom by its unique `leadId` (which must correspond to an existing lead), including contact information, status, associated opportunities, quotes/policies, custom fields, and interaction history. |
| `leads.getTheOpportunitiesForALead` | `agencyzoom.api.leads.getTheOpportunitiesForALead` | `read` | Retrieves all sales opportunities and their details for a specified, existing `leadId` in AgencyZoom. |
| `leads.moveLeadToSold` | `agencyzoom.api.leads.moveLeadToSold` | `write` | Marks an existing lead as sold by its `leadId` and records product details; `productLineId`, `premium`, `effectiveDate`, and `soldDate` are operationally required for each sold product, despite schema flexibility. |
| `leads.searchLeads` | `agencyzoom.api.leads.searchLeads` | `read` | Retrieves AgencyZoom leads, using filters, pagination, and sorting options sent in the POST body, as the request schema itself is empty. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `leads.searchLeadsCount` | `agencyzoom.api.leads.searchLeadsCount` | `read` | Retrieves a summary of lead counts categorized by workflow stage from AgencyZoom; this action does not support filtering and returns aggregate counts rather than individual lead details. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `leads.updateALeadFileName` | `agencyzoom.api.leads.updateALeadFileName` | `write` | Updates a lead's file name to `newFileName`; requires `fileId` of the target file, which must be associated with both the `leadId` (path parameter) and the provided `customerReferralId`. |
| `leads.updateALeadOpportunity` | `agencyzoom.api.leads.updateALeadOpportunity` | `write` | Updates an existing opportunity's details (carrier, product line, premium, items, custom fields) for a specific lead; `leadId`, opportunity `id`, `carrierId`, and `productLineId` must refer to existing entities. |
| `leads.updateALeadQuote` | `agencyzoom.api.leads.updateALeadQuote` | `write` | Updates an existing quote for a specified lead in AgencyZoom when its details require revision, ensuring the provided `leadId` (path parameter), quote `id` (body parameter), `carrierId`, and `productLineId` are valid and correspond to existing entities. |
| `leads.updateBusinessLead` | `agencyzoom.api.leads.updateBusinessLead` | `write` | Updates an existing business lead in AgencyZoom. Requires leadId (path parameter) plus core business information fields: firstname, email, name (business name), pipelineId, stageId, leadSourceId, assignTo (producer ID), and country. All other fields are optional and will only update if provided. |
| `leads.updateLead` | `agencyzoom.api.leads.updateLead` | `write` | Updates an existing lead's information in AgencyZoom using the `leadId`; ensure the `leadId` corresponds to an existing lead in AgencyZoom. |
| `leads.updateLeadStatusById` | `agencyzoom.api.leads.updateLeadStatusById` | `write` | Updates a lead's status (0=ACTIVE, 2=WON, 3=LOST, 5=XDATED) by `leadId`, optionally setting workflow, stage, date, loss reason, X-date type, source, recycle stage/pipeline, or tags; requires `date` and `xDateType` for status 5 (XDATED), and `lossReasonId` for status 3 (LOST). |
| `life.searchLifeAndHealthLeads` | `agencyzoom.api.life.searchLifeAndHealthLeads` | `read` | Searches for life and health insurance leads by providing filter criteria (matching AlrLead fields) in the request body; an empty request may retrieve all leads or a default set. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `opportunities.createADriverForAnOpportunity` | `agencyzoom.api.opportunities.createADriverForAnOpportunity` | `write` | Creates a new driver record associated with an existing AgencyZoom opportunity using its ID; requires firstName and lastName, with optional fields for additional driver details. |
| `opportunities.createAnOpportunity` | `agencyzoom.api.opportunities.createAnOpportunity` | `write` | Creates a new lead opportunity in AgencyZoom; this action cannot update existing opportunities. |
| `opportunities.createAVehicleForAnOpportunity` | `agencyzoom.api.opportunities.createAVehicleForAnOpportunity` | `write` | Adds a new vehicle record to a specified opportunity; `opportunityId` must refer to a valid, existing opportunity. |
| `opportunities.deleteADriver` | `agencyzoom.api.opportunities.deleteADriver` | `destructive` | Permanently deletes an existing driver record (a person associated with an insurance opportunity) from AgencyZoom using its unique `driverId`; this action is irreversible. |
| `opportunities.deleteAnOpportunity` | `agencyzoom.api.opportunities.deleteAnOpportunity` | `destructive` | Permanently deletes a specific opportunity by its unique ID; this action is irreversible and requires a valid, existing `opportunityId`. |
| `opportunities.deleteAVehicle` | `agencyzoom.api.opportunities.deleteAVehicle` | `destructive` | Permanently deletes a vehicle record by its `vehicleId`, which must correspond to an existing vehicle in the AgencyZoom system. |
| `opportunities.getAListOfDriversForAnOpportunity` | `agencyzoom.api.opportunities.getAListOfDriversForAnOpportunity` | `read` | Retrieves a list of drivers (individuals), including their personal details, licensing information, and relationship status, associated with a specific, existing `opportunityId` in AgencyZoom. |
| `opportunities.getAListOfVehiclesForAnOpportunity` | `agencyzoom.api.opportunities.getAListOfVehiclesForAnOpportunity` | `read` | Retrieves all vehicles associated with an existing opportunity, using its unique opportunityId. |
| `opportunities.getTheDriverDetails` | `agencyzoom.api.opportunities.getTheDriverDetails` | `read` | Retrieves detailed information for a specific, existing driver (by `driverId`) associated with an AgencyZoom opportunity. |
| `opportunities.getTheOpportunityDetails` | `agencyzoom.api.opportunities.getTheOpportunityDetails` | `read` | Fetches comprehensive details for an existing opportunity using its unique `opportunityId`. |
| `opportunities.getTheVehicleDetails` | `agencyzoom.api.opportunities.getTheVehicleDetails` | `read` | Retrieves detailed information for a specific vehicle, often associated with an AgencyZoom opportunity, using its unique vehicleId. |
| `opportunities.linkADriverToOpportunity` | `agencyzoom.api.opportunities.linkADriverToOpportunity` | `write` | Assigns or reassigns an existing driver to an existing opportunity. |
| `opportunities.linkAVehicleToOpportunity` | `agencyzoom.api.opportunities.linkAVehicleToOpportunity` | `write` | Links an existing vehicle to an existing sales opportunity in AgencyZoom using their respective IDs, typically for managing auto insurance policies or related services. |
| `opportunities.unlinkADriverFromOpportunity` | `agencyzoom.api.opportunities.unlinkADriverFromOpportunity` | `write` | Unlinks a currently associated driver from an AgencyZoom opportunity, used when the driver is no longer relevant, for policy or data updates, potentially impacting the opportunity's status or associated policy details. |
| `opportunities.unlinkAVehicleFromOpportunity` | `agencyzoom.api.opportunities.unlinkAVehicleFromOpportunity` | `write` | Unlinks a specific, existing vehicle from a specific, existing opportunity using their respective IDs, removing only the association and not the records themselves. |
| `opportunities.updateADriverSDetails` | `agencyzoom.api.opportunities.updateADriverSDetails` | `write` | Updates an existing driver's details in AgencyZoom, requiring `driverId` in the path and `firstName` and `lastName` in the request. |
| `opportunities.updateAnOpportunity` | `agencyzoom.api.opportunities.updateAnOpportunity` | `write` | Updates an existing opportunity with the provided details; ensure any custom field names are predefined in AgencyZoom configuration. |
| `opportunities.updateAVehicleSDetails` | `agencyzoom.api.opportunities.updateAVehicleSDetails` | `write` | Updates details for an existing vehicle within an opportunity; this action cannot be used to create new vehicle records. |
| `policies.updateAPolicy` | `agencyzoom.api.policies.updateAPolicy` | `write` | Updates an existing insurance policy for the given `policyId`; monetary values must be in cents and dates in YYYY-MM-DD format. |
| `policies.updateTagsForAPolicy` | `agencyzoom.api.policies.updateTagsForAPolicy` | `write` | Adds new comma-separated `tagNames` to a policy, requiring `tagNames` and identification by either `policyId` or `amsPolicyId`; if `amsPolicyId` is provided, `policyId` is ignored, and existing tags are not affected. |
| `profile.updateMyProfile` | `agencyzoom.api.profile.updateMyProfile` | `write` | Updates the profile information (first name, last name, email, and optional phone) for the currently authenticated user in AgencyZoom; the provided email address must be unique within the system. |
| `referenceData.getAListOfAssignGroups` | `agencyzoom.api.referenceData.getAListOfAssignGroups` | `read` | Retrieves all assign groups configured in AgencyZoom, used for categorizing or assigning items to specific teams or units. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfCarriers` | `agencyzoom.api.referenceData.getAListOfCarriers` | `read` | Retrieves all insurance carriers from AgencyZoom, typically used for populating selection lists or synchronizing carrier data; does not return detailed policy or coverage information. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfCsrs` | `agencyzoom.api.referenceData.getAListOfCsrs` | `read` | Fetches all Customer Service Representatives (CSRs), including their ID and name, returning an empty list if none are configured. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfCustomFields` | `agencyzoom.api.referenceData.getAListOfCustomFields` | `read` | Retrieves metadata for all configured custom field definitions, not the specific values entered for individual records. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfEmployees` | `agencyzoom.api.referenceData.getAListOfEmployees` | `read` | Retrieves a complete list of all employees for the authenticated agency; returns an empty list if no employees are configured. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfLeadSourceCategories` | `agencyzoom.api.referenceData.getAListOfLeadSourceCategories` | `read` | Retrieves a comprehensive list of all predefined lead source categories from AgencyZoom, used to classify lead origins. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfLeadSources` | `agencyzoom.api.referenceData.getAListOfLeadSources` | `read` | Fetches a list of all lead sources configured in AgencyZoom, including their ID, name, sales exclusion status, and category ID. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfLifeProfessionals` | `agencyzoom.api.referenceData.getAListOfLifeProfessionals` | `read` | Retrieves a list of life insurance professionals, including only their contact and status information (excluding sales or customer data), from the AgencyZoom platform. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfLocations` | `agencyzoom.api.referenceData.getAListOfLocations` | `read` | Retrieves all agency locations or branches from AgencyZoom; filtering options are not available. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfLossReasons` | `agencyzoom.api.referenceData.getAListOfLossReasons` | `read` | Retrieves all predefined loss reasons (e.g., 'Lost to competitor', 'Premium too high') available in AgencyZoom. These reasons can be used when marking leads as lost or for reporting purposes. No parameters required - returns complete list. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfPipelines` | `agencyzoom.api.referenceData.getAListOfPipelines` | `read` | Retrieves all pipelines and their stages from AgencyZoom to understand workflow structures; this is a read-only operation and does not return individual items (like leads or tasks) within these pipelines. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfProductLinesPolicyTypes` | `agencyzoom.api.referenceData.getAListOfProductLinesPolicyTypes` | `read` | Fetches all product lines and policy types from AgencyZoom, each detailed with its ID, name, and product category ID. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.getAListOfRecycleEvents` | `agencyzoom.api.referenceData.getAListOfRecycleEvents` | `read` | Retrieves the available recycle event types and any existing X-Date information for a specified lead in AgencyZoom. Returns a list of recycle event categories that can be used to trigger follow-ups (e.g., 'X-Date', 'Accident Falls Off', 'Prior Insurance Fulfilled'). |
| `referenceData.getDepartmentsGroups` | `agencyzoom.api.referenceData.getDepartmentsGroups` | `read` | Fetches department and group information for an agency, optionally filtered by a specific `agencyNumber`, to analyze its organizational structure. |
| `referenceData.getListOfEndStages` | `agencyzoom.api.referenceData.getListOfEndStages` | `read` | Fetches a list of all defined end stages, representing final steps in processes like lead conversion or policy closure. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.listProductCategories` | `agencyzoom.api.referenceData.listProductCategories` | `read` | Retrieves a complete, unfiltered list of all product categories (ID and name) from AgencyZoom, useful for understanding product organization or populating UI elements. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `referenceData.searchBusinessClassifications` | `agencyzoom.api.referenceData.searchBusinessClassifications` | `read` | Retrieves a comprehensive list of all available business classifications from AgencyZoom, each including an ID, code, and description. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `serviceTickets.serviceTicketList` | `agencyzoom.api.serviceTickets.serviceTicketList` | `read` | Retrieves a paginated list of service tickets from AgencyZoom. Supports filtering by: - Status (0=removed, 1=active, 2=completed) - Priority ID and Category ID Returns service ticket details including subject, description, dates, assignment information, and workflow stage. Results are paginated with default page size of 20 tickets per page. |
| `tasks.batchDeleteTask` | `agencyzoom.api.tasks.batchDeleteTask` | `destructive` | Deletes multiple AgencyZoom tasks in a batch; task deletion is permanent and the response indicates overall batch success, not individual task status. |
| `tasks.completeTask` | `agencyzoom.api.tasks.completeTask` | `write` | Marks an existing and accessible task in AgencyZoom as 'completed'; this action does not return the full updated task object. |
| `tasks.createTask` | `agencyzoom.api.tasks.createTask` | `write` | Creates a new task in AgencyZoom; ensure `assigneeId`, `customerId`, or `leadId` are valid existing entities if provided, and use `contactEmail`, `customerId`, or `leadId` to link the task to a contact. |
| `tasks.deleteATask` | `agencyzoom.api.tasks.deleteATask` | `destructive` | Permanently and irreversibly deletes an existing task, identified by its `taskId`. |
| `tasks.getTheTaskDetails` | `agencyzoom.api.tasks.getTheTaskDetails` | `read` | Retrieves comprehensive details for a specific task using its unique `taskId`, which must correspond to an existing task in AgencyZoom. |
| `tasks.reopenATask` | `agencyzoom.api.tasks.reopenATask` | `write` | Reopens an existing AgencyZoom task that is currently 'completed' or 'closed', allowing it to be reactivated with optional comments. |
| `tasks.searchTasks` | `agencyzoom.api.tasks.searchTasks` | `read` | Searches and lists tasks, supporting pagination and accepting filter criteria in the POST request body despite an empty request schema. |
| `tasks.updateTask` | `agencyzoom.api.tasks.updateTask` | `write` | Modifies an existing AgencyZoom task (which must be valid and identified by `taskId` in the path) with new attributes from the request body, which must also contain `taskId`. |
| `textThreads.getAListOfProducer` | `agencyzoom.api.textThreads.getAListOfProducer` | `read` | Retrieves a list of all producers from AgencyZoom, typically related to text thread functionalities. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `textThreads.removeTextThreadEndpoint` | `agencyzoom.api.textThreads.removeTextThreadEndpoint` | `destructive` | Call this action to permanently delete an SMS/text message thread in AgencyZoom; the target thread is identified by its `threadId`. Note: This operation is idempotent - deleting a non-existent thread will return success. |
| `textThreads.searchSmsThreads` | `agencyzoom.api.textThreads.searchSmsThreads` | `read` | Searches and retrieves SMS threads from AgencyZoom, with search parameters, filters, sorting, and pagination typically provided in the request body of this POST operation. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `textThreads.textDetailThread` | `agencyzoom.api.textThreads.textDetailThread` | `read` | Retrieves detailed messages for a specific SMS/text thread by its thread ID. Returns all messages in the thread with their content, attachments, sender/recipient information, and metadata. The thread ID can be obtained from the search_sms_threads action. |
| `textThreads.unreadThread` | `agencyzoom.api.textThreads.unreadThread` | `write` | Updates the read or unread status of a specific SMS/text thread within AgencyZoom. |
| `v4sso.authenticateForJwtviaV4Sso` | `agencyzoom.api.v4sso.authenticateForJwtviaV4Sso` | `write` | Completes AgencyZoom V4 Single Sign-On (SSO) authentication by exchanging an OAuth2 authorization code for a JWT token. Prerequisites: First call get_auth_url_for_v4sso to obtain the authentication URL, then redirect the user to that URL for login. After successful authentication through Vertafore's OAuth provider, the callback will include a 'code' parameter. Use that code with this action to obtain the JWT token for API access. Note: This is typically used during initial authentication setup. The code is single-use, time-limited, and must match the PKCE challenge from the original auth request. |
| `v4sso.getAuthUrlForV4Sso` | `agencyzoom.api.v4sso.getAuthUrlForV4Sso` | `write` | Retrieves a fresh authentication URL for AgencyZoom's V4 Single Sign-On (SSO) process; call before each SSO attempt as the URL may change and should not be cached. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |

## Auth

Auth: API key. Corsair prompts your tenant for credentials on first use.

## Webhooks

No webhooks.

## Reference

Full docs, types, and examples: https://docs.corsair.dev/plugins/agencyzoom

## License

Apache-2.0
