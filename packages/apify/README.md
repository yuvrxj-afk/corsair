# @corsair-dev/apify

Apify plugin for Corsair (MCP + REST API).

## Install

```bash
pnpm add @corsair-dev/apify
```

## Endpoints

| Operation | Operation ID | Risk | Description |
|-----------|--------------|------|-------------|
| `actorBuilds.abortActorBuild` | `apify.api.actorBuilds.abortActorBuild` | `write` | Tool to abort an Actor build that is starting or running. Use when you need to cancel a build in progress. Builds in terminal states (FINISHED, FAILED, ABORTING, TIMED-OUT) are not affected. |
| `actorBuilds.deleteActorBuild` | `apify.api.actorBuilds.deleteActorBuild` | `destructive` | Tool to delete an Actor build permanently. Use when you need to remove a specific build by its ID. The default build for an Actor cannot be deleted. Only users with build permissions can delete builds. |
| `actorBuilds.getActorBuild` | `apify.api.actorBuilds.getActorBuild` | `read` | Tool to get detailed information about a specific Actor build. Use when you need to retrieve complete build details by build ID. Optionally wait for the build to finish using the waitForFinish parameter to avoid polling. |
| `actorBuilds.getActorBuildLog` | `apify.api.actorBuilds.getActorBuildLog` | `read` | Tool to retrieve the log file for a specific Actor build. Use when you need to inspect logs generated during an Actor build process. Only the trailing 5 million characters of the log are stored. |
| `actorBuilds.getUserBuildsList` | `apify.api.actorBuilds.getUserBuildsList` | `read` | Tool to get a paginated list of all builds for a user. Use when you need to retrieve build history across all actors. Supports pagination up to 1000 records. |
| `actorRuns.abortActorRun` | `apify.api.actorRuns.abortActorRun` | `write` | Tool to abort a running or starting Actor run. Use when you need to stop an Actor run that is currently in STARTING or RUNNING status. For runs with status FINISHED, FAILED, ABORTING, and TIMED-OUT this call does nothing. |
| `actorRuns.deleteActorRun` | `apify.api.actorRuns.deleteActorRun` | `destructive` | Tool to delete a finished Actor run. Use when you need to permanently remove a completed run. Only finished runs can be deleted by the initiating user or organization. |
| `actorRuns.getActorRun` | `apify.api.actorRuns.getActorRun` | `read` | Tool to get details about a specific Actor run. Use when you need to retrieve comprehensive information about a run including its execution status, resource usage, storage IDs, and metadata. |
| `actorRuns.getRunDatasetItems` | `apify.api.actorRuns.getRunDatasetItems` | `read` | Tool to get dataset items from a specific Actor run. Use when you need to retrieve the output data from a completed or running Actor run. |
| `actorRuns.listUserActorRuns` | `apify.api.actorRuns.listUserActorRuns` | `read` | Tool to get a paginated list of all Actor runs for the authenticated user. Use when you need to browse all runs across all actors, optionally filtered by status or date range. |
| `actorRuns.resurrectRun` | `apify.api.actorRuns.resurrectRun` | `write` | Tool to resurrect a finished Actor run. Use when you need to restart a completed or failed run. Deprecated endpoint; may be removed in future. |
| `actorRuns.updateActorRunStatusMessage` | `apify.api.actorRuns.updateActorRunStatusMessage` | `write` | Tool to update the status message of an Actor run. Use when you need to set progress information or status updates that will be displayed in the Apify Console UI during Actor execution. |
| `actors.callActor` | `apify.api.actors.callActor` | `write` | Run an Apify Actor and return run metadata |
| `actors.fetchActorDetails` | `apify.api.actors.fetchActorDetails` | `read` | Get detailed information about an Actor including schemas and pricing |
| `actors.ragWebBrowser` | `apify.api.actors.ragWebBrowser` | `write` | Search the web and scrape top results using the RAG Web Browser Actor |
| `actors.searchActors` | `apify.api.actors.searchActors` | `read` | Search for Actors in the Apify Store |
| `actorTasks.createActorTask` | `apify.api.actorTasks.createActorTask` | `write` | Tool to create a new Actor task with specified settings. Use when you need to configure or schedule recurring Actor runs programmatically. |
| `actorTasks.deleteActorTask` | `apify.api.actorTasks.deleteActorTask` | `destructive` | Tool to delete an Actor task permanently. Use when you need to remove a task by its ID or username~taskName. Confirm before calling. |
| `actorTasks.getActorTask` | `apify.api.actorTasks.getActorTask` | `read` | Tool to get complete details about an Actor task. Use when you need to retrieve task configuration, input settings, or metadata by task ID or username~task-name. |
| `actorTasks.getLastActorTaskRun` | `apify.api.actorTasks.getLastActorTaskRun` | `read` | Tool to get the most recent run of a specific Actor task. Use when you need to retrieve the last execution details. You can filter by status to get only successful runs using status='SUCCEEDED'. |
| `actorTasks.getListOfTaskRuns` | `apify.api.actorTasks.getListOfTaskRuns` | `read` | Tool to get a list of runs for a specific Actor task. Use when you need to paginate through task runs and optionally filter by status. |
| `actorTasks.getListOfTasks` | `apify.api.actorTasks.getListOfTasks` | `read` | Tool to fetch a paginated list of tasks belonging to the authenticated user. Use when you need to browse or sort tasks created by the user. |
| `actorTasks.getListOfTaskWebhooks` | `apify.api.actorTasks.getListOfTaskWebhooks` | `read` | Tool to get a list of webhooks for a specific Actor task. Use when you need to review or paginate webhooks after creating or updating a task. |
| `actorTasks.getTaskInput` | `apify.api.actorTasks.getTaskInput` | `read` | Tool to retrieve the input configuration of a specific task. Use when you need to inspect stored task input before execution or debugging. |
| `actorTasks.getTaskLastRunDatasetItems` | `apify.api.actorTasks.getTaskLastRunDatasetItems` | `read` | Tool to get dataset items from the last run of an Actor task. Use when you need to retrieve data from the most recent task execution. Filter by status (e.g., 'SUCCEEDED') to get items from the last successful run only. |
| `actorTasks.runTaskAsynchronously` | `apify.api.actorTasks.runTaskAsynchronously` | `write` | Tool to run a specific Actor task asynchronously. Use when you need to trigger a task run without waiting for completion and immediately retrieve its run details. |
| `actorTasks.runTaskSyncGet` | `apify.api.actorTasks.runTaskSyncGet` | `write` | Tool to run a specific task synchronously and return its output. Use when immediate task results are needed with pre-configured settings. The run must finish within 300 seconds otherwise the HTTP request fails with a timeout error. |
| `actorTasks.runTaskSyncGetDatasetItems` | `apify.api.actorTasks.runTaskSyncGetDatasetItems` | `write` | Tool to run an actor task synchronously and retrieve its dataset items. Use when immediate access to task run results is needed. The run must finish within 300 seconds otherwise the request times out. For large datasets exceeding the timeout, use `limit`/`offset` pagination to retrieve results in smaller batches or switch to an async run pattern with a separate dataset retrieval call. |
| `actorTasks.runTaskSyncPost` | `apify.api.actorTasks.runTaskSyncPost` | `write` | Tool to run an Actor task synchronously with input override and return its output. Use when immediate task results are needed with custom input parameters. The run must finish within 300 seconds otherwise the HTTP request fails with a timeout error (though the run continues server-side). |
| `actorTasks.runTaskSyncWithInputOverrideGetDatasetItems` | `apify.api.actorTasks.runTaskSyncWithInputOverrideGetDatasetItems` | `write` | Tool to run an actor task synchronously with input overrides and retrieve its dataset items. Use when you need to override task input configuration and get immediate results. The run must finish within 300 seconds otherwise the request times out. |
| `actorTasks.updateActorTask` | `apify.api.actorTasks.updateActorTask` | `write` | Tool to update Actor task settings using JSON payload. Only specified properties are updated; others remain unchanged. Use when you need to modify task configuration, input, or execution options. |
| `actorTasks.updateTaskInput` | `apify.api.actorTasks.updateTaskInput` | `write` | Tool to update the input configuration of a specific Actor task. Use when you need to modify a scheduled task input before execution. |
| `acts.buildActor` | `apify.api.acts.buildActor` | `write` | Tool to build an Actor with specified configuration. Use when you need to create a new build of an Actor with a specific version. The build process compiles the Actor's source code into a Docker image. |
| `acts.createActor` | `apify.api.acts.createActor` | `write` | Tool to create a new Actor with specified configuration. Use when you need to initialize a fresh Actor programmatically before publishing or running it. |
| `acts.createActorVersion` | `apify.api.acts.createActorVersion` | `write` | Tool to create a new version of an Actor. Use when you need to add a new version with specific source code location and configuration. Requires versionNumber and sourceType parameters, plus conditional parameters based on the sourceType. |
| `acts.createActorVersionEnvironmentVariable` | `apify.api.acts.createActorVersionEnvironmentVariable` | `write` | Tool to create an environment variable for a specific Actor version. Use when adding new environment variables to Actor versions. Requires name and value parameters. |
| `acts.deleteActor` | `apify.api.acts.deleteActor` | `destructive` | Tool to delete an Actor permanently. Use when you need to remove an Actor by its ID or username~actorName. Confirm before calling. |
| `acts.deleteActorVersion` | `apify.api.acts.deleteActorVersion` | `destructive` | Tool to delete a specific version of an Actor's source code. Use when you need to remove an Actor version by actor ID and version number. Confirm before calling. |
| `acts.deleteActorVersionEnvironmentVariable` | `apify.api.acts.deleteActorVersionEnvironmentVariable` | `destructive` | Tool to delete an environment variable from a specific Actor version. Use when removing environment variables from Actor versions. |
| `acts.getActorDetails` | `apify.api.acts.getActorDetails` | `read` | Tool to get details of a specific Actor. Use when you need actor metadata by ID or username/actorName. Response includes `isDeprecated` and `notice` fields indicating deprecation status, and `pricingInfos` for per-unit cost details — review both before scheduling runs. |
| `acts.getActorLastRunDatasetItems` | `apify.api.acts.getActorLastRunDatasetItems` | `read` | Tool to get dataset items from the last run of an Actor. Use when you need to retrieve output data from the most recent Actor execution, optionally filtered by run status (e.g., status='SUCCEEDED' to get items only from the last successful run). |
| `acts.getActorVersion` | `apify.api.acts.getActorVersion` | `read` | Tool to get details about a specific version of an Actor. Use when you need version metadata including source type, build tag, and configuration details. |
| `acts.getActorVersionEnvironmentVariable` | `apify.api.acts.getActorVersionEnvironmentVariable` | `read` | Tool to get environment variable details for a specific Actor version. Use when retrieving environment variable information from an Actor version. Returns name, value (if not secret), and secret status. |
| `acts.getDefaultBuild` | `apify.api.acts.getDefaultBuild` | `read` | Tool to get the default build for an Actor. Use after specifying the Actor ID; optionally wait for the build to finish before returning. |
| `acts.getLastActorRun` | `apify.api.acts.getLastActorRun` | `read` | Tool to get the most recent run of a specific Actor. Use when you need to retrieve the last execution details of an Actor and optionally filter by status (e.g., status='SUCCEEDED' to get only the last successful run). |
| `acts.getListOfActors` | `apify.api.acts.getListOfActors` | `read` | Tool to get the list of all Actors that the user created or used. Use when you need to enumerate or browse Actors. Add my=1 to get only user-created Actors. |
| `acts.getListOfActorVersionEnvironmentVariables` | `apify.api.acts.getListOfActorVersionEnvironmentVariables` | `read` | Tool to get the list of environment variables for a specific Actor version. Use when you need to retrieve environment variable configurations for an Actor version. |
| `acts.getListOfActorVersions` | `apify.api.acts.getListOfActorVersions` | `read` | Tool to get the list of versions of a specific Actor. Use when you need to retrieve version metadata including source type, version number, and configuration details. |
| `acts.getListOfActorWebhooks` | `apify.api.acts.getListOfActorWebhooks` | `read` | Tool to get a list of webhooks for a specific Actor. Use when you need to review or manage webhooks configured for an Actor. |
| `acts.getListOfBuilds` | `apify.api.acts.getListOfBuilds` | `read` | Tool to get a list of builds for a specific Actor. Use when you need paginated access to an Actor’s build (version) history. |
| `acts.getListOfRuns` | `apify.api.acts.getListOfRuns` | `read` | Tool to get a list of runs for a specific Actor. Use when you need to paginate through runs and optionally filter by status before processing run data. |
| `acts.getOpenapiDefinition` | `apify.api.acts.getOpenapiDefinition` | `read` | Tool to get the OpenAPI definition for a specific Actor build. Use when you need the API schema for code generation or analysis. |
| `acts.runActorAsynchronously` | `apify.api.acts.runActorAsynchronously` | `write` | Tool to run a specific Actor asynchronously. Use when you need to trigger an Actor run without waiting for completion and retrieve its run details immediately. |
| `acts.runActorSync` | `apify.api.acts.runActorSync` | `write` | Tool to run a specific Actor synchronously with input and return its output record. Use when immediate Actor results are needed; runs may timeout after 300 seconds. To avoid timeouts, scope inputs to specific URLs rather than broad crawls and request only necessary fields (e.g., text or markdown). |
| `acts.runActorSyncGetDatasetItems` | `apify.api.acts.runActorSyncGetDatasetItems` | `write` | Tool to run Actor synchronously and get dataset items. Supports both actors that require input and those that don't. Use when immediate access to Actor results is needed. The run must finish within 300 seconds otherwise the request times out. |
| `acts.runActorSyncGetDatasetItemsItems` | `apify.api.acts.runActorSyncGetDatasetItemsItems` | `write` | Tool to run an Actor synchronously and retrieve its dataset items. Use when immediate access to run results is needed. |
| `acts.runActorSyncWithoutInputGet` | `apify.api.acts.runActorSyncWithoutInputGet` | `write` | Tool to run a specific Actor synchronously without input and return its output. Use when immediate Actor results are needed without providing input data; the run must finish within 300 seconds otherwise the HTTP request fails with a timeout error. |
| `acts.updateActor` | `apify.api.acts.updateActor` | `write` | Tool to update Actor settings using JSON payload. Only specified fields will be updated. Use when you need to modify Actor configuration, make an Actor public, or update version settings. |
| `acts.updateActorVersion` | `apify.api.acts.updateActorVersion` | `write` | Tool to update an Actor version's configuration and source code. Use when modifying version properties such as buildTag, sourceType, or environment variables. Only specified properties will be updated. |
| `acts.updateActorVersionEnvironmentVariable` | `apify.api.acts.updateActorVersionEnvironmentVariable` | `write` | Tool to update environment variable for a specific Actor version using JSON payload. Only specified fields will be updated. Use when modifying existing environment variables in Actor versions. |
| `datasets.createDataset` | `apify.api.datasets.createDataset` | `write` | Tool to create a new dataset. Use when you need to initialize or retrieve a dataset by name. |
| `datasets.deleteDataset` | `apify.api.datasets.deleteDataset` | `destructive` | Tool to delete a dataset permanently. Use when you need to remove a dataset by its ID or username~dataset-name. Confirm before calling. |
| `datasets.getDataset` | `apify.api.datasets.getDataset` | `read` | Tool to retrieve dataset metadata by dataset ID. Use when you need information about a dataset's structure, item counts, or access URLs. This does not return dataset items themselves. |
| `datasets.getDatasetItems` | `apify.api.datasets.getDatasetItems` | `read` | Tool to retrieve items from a dataset. Use when you need to fetch data from a specified dataset by pagination or filtering. Only JSON format is fully supported. For datasets larger than 1000 items, issue multiple calls incrementing `offset` by `limit` until the response returns fewer items than `limit`. |
| `datasets.getDatasetStatistics` | `apify.api.datasets.getDatasetStatistics` | `read` | Tool to get dataset field statistics by dataset ID. Use when you need statistical information about dataset fields including min, max, null count, and empty count. Only provides field statistics when dataset schema is configured. |
| `datasets.getListOfDatasets` | `apify.api.datasets.getListOfDatasets` | `read` | Tool to get list of datasets for a user. Use when you need to enumerate or browse user's datasets. Supports pagination with up to 1000 items per page. |
| `datasets.storeDataInDataset` | `apify.api.datasets.storeDataInDataset` | `write` | Tool to store data items in a dataset. Use after collecting data when you want to batch-append or update items in an existing dataset. |
| `datasets.updateDataset` | `apify.api.datasets.updateDataset` | `write` | Tool to update a dataset's name via JSON payload. Use when you need to rename an existing dataset. |
| `docs.fetchApifyDocs` | `apify.api.docs.fetchApifyDocs` | `read` | Fetch the full content of an Apify documentation page |
| `docs.searchApifyDocs` | `apify.api.docs.searchApifyDocs` | `read` | Search Apify and Crawlee documentation |
| `keyValueStores.checkKeyValueStoreRecordExists` | `apify.api.keyValueStores.checkKeyValueStoreRecordExists` | `read` | Tool to check if a record exists in a key-value store. Use when you need to verify whether a specific key exists in an Apify Key-Value Store without retrieving its content. |
| `keyValueStores.createKeyValueStore` | `apify.api.keyValueStores.createKeyValueStore` | `write` | Tool to create a new key-value store or retrieve an existing one by name. Use when you need to initialize a store for saving data records or files. If a store with the given name already exists, returns that store instead of creating a duplicate. |
| `keyValueStores.deleteKeyValueStore` | `apify.api.keyValueStores.deleteKeyValueStore` | `destructive` | Tool to delete a key-value store permanently. Use when you need to remove a key-value store by its ID. Confirm before calling. |
| `keyValueStores.deleteKeyValueStoreRecord` | `apify.api.keyValueStores.deleteKeyValueStoreRecord` | `destructive` | Tool to delete a record from a key-value store. Use when you need to remove a specific record by its key from an Apify Key-Value Store. |
| `keyValueStores.getKeyValueRecord` | `apify.api.keyValueStores.getKeyValueRecord` | `read` | Tool to retrieve a record from a key-value store. Use when you need to fetch a specific value by key from an Apify Key-Value Store. |
| `keyValueStores.getKeyValueStore` | `apify.api.keyValueStores.getKeyValueStore` | `read` | Tool to retrieve key-value store metadata by store ID. Use when you need detailed information about a specific key-value store including stats and access URLs. |
| `keyValueStores.getKeyValueStoreKeys` | `apify.api.keyValueStores.getKeyValueStoreKeys` | `read` | Tool to retrieve a list of keys from a key-value store. Use when you need to list keys in a store with optional filtering and pagination support. |
| `keyValueStores.getListOfKeyValueStores` | `apify.api.keyValueStores.getListOfKeyValueStores` | `read` | Tool to get the list of key-value stores owned by the user. Use when you need to enumerate or browse available stores. Supports pagination up to 1000 records per request. |
| `keyValueStores.storeDataInKeyValueStore` | `apify.api.keyValueStores.storeDataInKeyValueStore` | `write` | Tool to create or update a record in a key-value store. Use after you have the store ID and record key to persist JSON data. |
| `keyValueStores.updateKeyValueStore` | `apify.api.keyValueStores.updateKeyValueStore` | `write` | Tool to update a key-value store's properties. Use when renaming or changing access or schema version of the store after confirming the store ID. |
| `logs.getLog` | `apify.api.logs.getLog` | `read` | Tool to retrieve logs for a specific Actor run or build. Use after a run completes or fails — a run may report success status yet contain only informational messages, making log inspection the only way to confirm actual outcomes. For long runs, log responses can be very large; prioritize error-level entries and recent timestamps to diagnose issues efficiently. |
| `requestQueues.addRequestToQueue` | `apify.api.requestQueues.addRequestToQueue` | `write` | Tool to add a request to the queue. Use when you need to add a web page URL to a request queue for crawling. If a request with the same uniqueKey was already present in the queue, returns the ID of the existing request. |
| `requestQueues.batchAddRequestsToQueue` | `apify.api.requestQueues.batchAddRequestsToQueue` | `write` | Tool to batch-add up to 25 requests to a request queue. Use when you need to add multiple requests efficiently. Failed requests due to rate limits should be retried with exponential backoff. |
| `requestQueues.batchDeleteRequestsFromQueue` | `apify.api.requestQueues.batchDeleteRequestsFromQueue` | `destructive` | Tool to batch-delete up to 25 requests from a queue. Use when you need to remove multiple requests efficiently. Failed requests due to rate limits should be retried with exponential backoff. |
| `requestQueues.createRequestQueue` | `apify.api.requestQueues.createRequestQueue` | `write` | Tool to create a new request queue or retrieve an existing one by name. Use when you need to initialize a queue for storing and managing web scraping requests. If a queue with the given name already exists, returns that queue instead of creating a duplicate. Unnamed queues follow data retention period policies. |
| `requestQueues.deleteRequestFromQueue` | `apify.api.requestQueues.deleteRequestFromQueue` | `destructive` | Tool to delete a specific request from a request queue. Use when you need to remove a request by its ID from an Apify request queue. |
| `requestQueues.deleteRequestLock` | `apify.api.requestQueues.deleteRequestLock` | `write` | Tool to delete a request lock from a request queue. Use when you need to unlock a previously locked request. Only the client that locked the request can delete its lock. |
| `requestQueues.deleteRequestQueue` | `apify.api.requestQueues.deleteRequestQueue` | `destructive` | Tool to delete a request queue permanently. Use when you need to remove a request queue by its ID. |
| `requestQueues.getHeadAndLockQueueRequests` | `apify.api.requestQueues.getHeadAndLockQueueRequests` | `write` | Tool to get and lock head requests from the queue. Returns the given number of first requests from the queue and locks them for the given time, preventing other clients from accessing them during the lock period. Use when you need to process requests exclusively without concurrent access by other clients. |
| `requestQueues.getListOfRequestQueues` | `apify.api.requestQueues.getListOfRequestQueues` | `read` | Tool to get list of request queues for a user. Use when you need to enumerate or browse user's request queues. Supports pagination with up to 1000 items per page. |
| `requestQueues.getRequestFromQueue` | `apify.api.requestQueues.getRequestFromQueue` | `read` | Tool to retrieve a specific request from a request queue by its ID. Use when you need to get detailed information about a request in an Apify request queue. |
| `requestQueues.getRequestQueue` | `apify.api.requestQueues.getRequestQueue` | `read` | Tool to retrieve request queue metadata by queue ID. Use when you need information about a specific request queue including its statistics and request counts. |
| `requestQueues.getRequestQueueHead` | `apify.api.requestQueues.getRequestQueueHead` | `read` | Tool to retrieve first requests from the queue for inspection. Use when you need to examine pending requests without locking them. |
| `requestQueues.listRequestQueueRequests` | `apify.api.requestQueues.listRequestQueueRequests` | `read` | Tool to list requests in a request queue with pagination support. Use when you need to retrieve multiple requests from an Apify request queue. |
| `requestQueues.prolongRequestLock` | `apify.api.requestQueues.prolongRequestLock` | `write` | Tool to prolong request lock in a request queue. Use when you need to extend the lock duration on a previously locked request. Only the client that locked the request can prolong its lock. |
| `requestQueues.unlockQueueRequests` | `apify.api.requestQueues.unlockQueueRequests` | `write` | Tool to unlock requests in a request queue that are currently locked by the client. If the client is within an Actor run, unlocks all requests locked by that specific run plus all requests locked by the same clientKey. If the client is outside of an Actor run, unlocks all requests locked using the same clientKey. |
| `requestQueues.updateRequestInQueue` | `apify.api.requestQueues.updateRequestInQueue` | `write` | Tool to update a request in a request queue. Use when you need to modify request properties or mark a request as handled by setting handledAt to the current date/time. If handledAt is set, the request will be removed from the head of the queue and unlocked if applicable. |
| `requestQueues.updateRequestQueue` | `apify.api.requestQueues.updateRequestQueue` | `write` | Tool to update request queue name using JSON payload. Use when you need to rename an existing request queue. |
| `runs.getActorOutput` | `apify.api.runs.getActorOutput` | `read` | Retrieve dataset items from an Actor run (Apify MCP get-dataset-items) |
| `runs.getActorRun` | `apify.api.runs.getActorRun` | `read` | Get detailed information about a specific Actor run |
| `schedules.createSchedule` | `apify.api.schedules.createSchedule` | `write` | Tool to create a new schedule with specified settings. Use when you need to automate Actor or Actor task execution at specific times using cron expressions. |
| `schedules.deleteSchedule` | `apify.api.schedules.deleteSchedule` | `destructive` | Tool to delete a schedule by its ID. Use when you need to remove a schedule from the Apify system. |
| `schedules.getListOfSchedules` | `apify.api.schedules.getListOfSchedules` | `read` | Tool to get list of schedules created by the user. Use when you need to browse or enumerate user's schedules. Supports pagination with up to 1000 items per page. |
| `schedules.getSchedule` | `apify.api.schedules.getSchedule` | `read` | Tool to get schedule details by ID. Use when you need to retrieve comprehensive information about a schedule including cron expression, timezone, actions, and execution times. |
| `schedules.getScheduleLog` | `apify.api.schedules.getScheduleLog` | `read` | Tool to get schedule log by ID. Use when you need to retrieve execution history for a schedule, including invocation timestamps and status messages. Returns up to 1000 invocations. |
| `schedules.updateSchedule` | `apify.api.schedules.updateSchedule` | `write` | Tool to update an existing schedule with new settings. Use when you need to modify schedule properties like cron expression, timezone, enabled status, or actions. Only specified fields are updated; others remain unchanged. |
| `store.getListOfActorsInStore` | `apify.api.store.getListOfActorsInStore` | `read` | Tool to get list of public Actors from Apify Store. Use when you need to browse or search public Actors available in the store. Supports searching by title, name, description, username, and readme. |
| `users.getAccountLimits` | `apify.api.users.getAccountLimits` | `read` | Tool to get a complete summary of account limits and usage. Use when you need to retrieve information about usage cycles, spending caps, compute resources, data transfer quotas, and other account limits. This shows the same information as the Limits page in Apify console. |
| `users.getCurrentUserAccountData` | `apify.api.users.getCurrentUserAccountData` | `read` | Tool to get private user account information. Use when you need to retrieve comprehensive data about the current user identified by the authentication token, including profile, subscription plan, and proxy settings. Note: 'plan', 'email', and 'profile' fields are omitted when accessed from Actor run. |
| `users.getMonthlyUsage` | `apify.api.users.getMonthlyUsage` | `read` | Tool to get monthly usage summary with daily breakdown. Use when you need detailed usage information including storage, data transfer, and request queue metrics for the current or a specific billing cycle. This shows the same information as the Billing page in Apify console. |
| `users.getPublicUserData` | `apify.api.users.getPublicUserData` | `read` | Tool to get public user data. Use when you need to retrieve publicly accessible information about a specific Apify user account, similar to what can be seen on public profile pages. This operation requires no authentication token. |
| `users.updateAccountLimits` | `apify.api.users.updateAccountLimits` | `write` | Tool to update account limits manageable on the Limits page. Use when you need to set or modify the monthly spending cap (maxMonthlyUsageUsd) or data retention period (dataRetentionDays). At least one limit parameter must be provided. |
| `webhookDispatches.getListOfWebhookDispatches` | `apify.api.webhookDispatches.getListOfWebhookDispatches` | `read` | Tool to get list of webhook dispatches for the user. Use when you need to retrieve webhook execution history with pagination support. |
| `webhookDispatches.getWebhookDispatch` | `apify.api.webhookDispatches.getWebhookDispatch` | `read` | Tool to get webhook dispatch object with all details. Use when you need to retrieve information about a specific webhook dispatch including its status, event data, and call history. |
| `webhooks.createTaskWebhook` | `apify.api.webhooks.createTaskWebhook` | `write` | Tool to create a webhook for an Actor task. Use when you need external notifications about task run events (e.g., completion or failure) in downstream systems. |
| `webhooks.deleteWebhook` | `apify.api.webhooks.deleteWebhook` | `destructive` | Tool to delete a webhook by its ID. Use when removing a webhook after confirming the webhook ID. |
| `webhooks.getAllWebhooks` | `apify.api.webhooks.getAllWebhooks` | `read` | Tool to get a list of all webhooks created by the user. Use when you need to enumerate webhooks before filtering or maintenance. |
| `webhooks.getWebhook` | `apify.api.webhooks.getWebhook` | `read` | Tool to get webhook object with all details. Use when you need to retrieve complete information about a specific webhook by its ID. |
| `webhooks.getWebhookDispatches` | `apify.api.webhooks.getWebhookDispatches` | `read` | Tool to get list of webhook dispatches for a specific webhook. Use when you need to retrieve dispatch history for a particular webhook with pagination support. |
| `webhooks.testWebhook` | `apify.api.webhooks.testWebhook` | `write` | Tool to test a webhook by creating a test dispatch with a dummy payload. Use when you need to verify webhook configuration before production use. |
| `webhooks.updateWebhook` | `apify.api.webhooks.updateWebhook` | `write` | Tool to update webhook using JSON payload. Only specified properties are updated; others remain unchanged. Use when you need to modify webhook settings like event types, target URL, or other configuration. |

## Auth

Auth: API key. Corsair prompts your tenant for credentials on first use.

## Webhooks

No webhooks.

## Reference

Full docs, types, and examples: https://docs.corsair.dev/plugins/apify

## License

Apache-2.0
