# @corsair-dev/affinda

Affinda plugin for Corsair.

## Install

```bash
pnpm add @corsair-dev/affinda
```

## Endpoints

| Operation | Operation ID | Risk | Description |
|-----------|--------------|------|-------------|
| `annotations.batchUpdateAnnotations` | `affinda.api.annotations.batchUpdateAnnotations` | `write` | Batch update multiple document annotations in a single API call. Use this action to efficiently update parsed values or other fields across many annotations at once, rather than making individual update requests for each annotation. Prerequisites: - Obtain annotation IDs using the 'Get Annotations' action with a document filter - Annotations must exist in documents that have been processed by Affinda Common use cases: - Correcting OCR/extraction errors in bulk - Updating parsed values after manual review - Modifying annotation data programmatically |
| `annotations.createBatchAnnotations` | `affinda.api.annotations.createBatchAnnotations` | `write` | Batch create multiple document annotations in a single API call. Use this action to efficiently create multiple annotations at once for documents that have been processed by Affinda. This is useful for programmatically adding structured data to documents or importing annotation data from external sources. Prerequisites: - Documents must be created first using 'Create Document' action - Obtain document identifiers from 'Create Document' or 'Get Documents' actions - Know the data point identifiers for your collection (from extractor configuration) Common use cases: - Importing annotation data from external systems - Programmatically adding structured data to documents - Creating annotations for validation or training purposes |
| `annotations.deleteAnnotationsBatch` | `affinda.api.annotations.deleteAnnotationsBatch` | `destructive` | Batch delete multiple document annotations in a single API call. Use this action to efficiently remove multiple annotations at once rather than making individual delete requests for each annotation. Prerequisites: - Obtain annotation IDs using the 'Get Annotations' action with a document filter - Annotations must exist in documents that have been processed by Affinda Common use cases: - Removing incorrect or unwanted annotations in bulk - Cleaning up annotations after document reprocessing - Deleting specific extracted data fields programmatically |
| `annotations.getAnnotations` | `affinda.api.annotations.getAnnotations` | `read` | Retrieves all annotations for a specific document. Annotations are extracted data fields (text, dates, numbers, tables, etc.) from parsed documents. Use this after a document has been processed to access the extracted data. |
| `annotations.updateAnnotation` | `affinda.api.annotations.updateAnnotation` | `write` | Tool to update data of a single annotation in Affinda. Use when you need to correct extracted values, verify annotations, or modify annotation metadata after document processing. Prerequisites: - Obtain the annotation ID using the 'Get Annotations' action with a document filter - Annotations must exist in documents that have been processed by Affinda Common use cases: - Correcting OCR or extraction errors in individual fields - Marking annotations as verified after manual review (set isClientVerified=true) - Updating parsed values to fix incorrect data extraction - Modifying annotation coordinates or page references Note: For bulk updates across multiple annotations, use the 'Batch Update Annotations' action instead for better efficiency. |
| `apiUsers.createApiUser` | `affinda.api.apiUsers.createApiUser` | `write` | Tool to create a new API user within an organization. Use when you need to generate a new API user with authentication credentials for programmatic access to Affinda services. |
| `apiUsers.getAllApiUsers` | `affinda.api.apiUsers.getAllApiUsers` | `read` | Tool to retrieve a list of all API users. Use when you need to list API users, optionally filtered by organization. |
| `collections.createCollection` | `affinda.api.collections.createCollection` | `write` | Tool to create a new collection. Use after you have a valid workspace ID and want to group documents by a specific extractor within that workspace. |
| `collections.createDataFieldForCollection` | `affinda.api.collections.createDataFieldForCollection` | `write` | Tool to create a data field for a collection along with a new data point. Use when you need to add a custom field to a collection for document processing and validation. |
| `collections.deleteCollection` | `affinda.api.collections.deleteCollection` | `destructive` | Permanently delete a collection from Affinda by its identifier. This is a destructive operation that cannot be undone. Use Get Collections first to verify the collection ID before deletion. Note: Documents in the collection may also be affected. |
| `collections.getCollection` | `affinda.api.collections.getCollection` | `read` | Tool to retrieve details of a specific collection by its ID. Use when you need full metadata about a collection after confirming its identifier. |
| `collections.getCollectionFields` | `affinda.api.collections.getCollectionFields` | `read` | Tool to retrieve data field configuration for a collection associated with a data point. Use when you need to understand the structure and validation rules of a specific field in a collection. Note: This endpoint is deprecated. |
| `collections.getCollections` | `affinda.api.collections.getCollections` | `read` | Tool to retrieve a list of all collections. Use when you need to list collections in your Affinda workspace after authentication. |
| `collections.getCollectionUsage` | `affinda.api.collections.getCollectionUsage` | `read` | Retrieves monthly credits consumption statistics for a specific collection. Returns a list of usage records showing how many credits were consumed each month. Use this to track collection activity, monitor consumption trends, or generate usage reports. Note: This endpoint is deprecated. |
| `collections.updateCollection` | `affinda.api.collections.updateCollection` | `write` | Tool to update specific fields of a collection. Use when you need to rename or reassign a collection after creation. |
| `collections.updateDataFieldForCollection` | `affinda.api.collections.updateDataFieldForCollection` | `write` | Tool to update a data field configuration for a collection's data point. Use when you need to modify field properties such as label, type, mandatory status, or validation settings after the field has been created. |
| `dataPointChoices.createDataPointChoice` | `affinda.api.dataPointChoices.createDataPointChoice` | `write` | Tool to create a custom data point choice. Use when you need to add a new choice option for a specific data point in a collection or organization. Note: This endpoint is deprecated but still functional. |
| `dataPointChoices.getDataPointChoice` | `affinda.api.dataPointChoices.getDataPointChoice` | `read` | Tool to return a specific data point choice by its ID. Use when you need details about a particular data point choice option. Note: This endpoint is deprecated but still functional. |
| `dataPointChoices.listDataPointChoices` | `affinda.api.dataPointChoices.listDataPointChoices` | `read` | Tool to retrieve available choices for a specific enum data point. Use when you need to discover valid options for dropdown/enum fields in a collection's data schema. Note: This endpoint is deprecated but still functional for accessing data point choice metadata. |
| `dataPointChoices.replaceDataPointChoices` | `affinda.api.dataPointChoices.replaceDataPointChoices` | `write` | Tool to completely replace all choices for a data point. Use when you need to bulk update enum options - existing choices are matched by value, new values are created, updated values are modified, and values not in the incoming list are deleted. Note: This is a deprecated endpoint but still functional. Either collection or organization must be specified to scope the replacement. |
| `dataPointChoices.updateDataPointChoice` | `affinda.api.dataPointChoices.updateDataPointChoice` | `write` | Tool to update data of a data point choice by its ID. Use when you need to modify label, value, description, synonyms, or associated collection/organization for an existing data point choice. Note: This endpoint is deprecated but still functional. |
| `dataPoints.createDataPoint` | `affinda.api.dataPoints.createDataPoint` | `write` | Tool to create a custom data point for document extraction. Use when you need to define a new field that should be extracted from documents in a specific extractor. Note: This endpoint is deprecated but still functional. Data points define custom fields for extraction models. |
| `dataPoints.deleteDataPoint` | `affinda.api.dataPoints.deleteDataPoint` | `destructive` | Tool to permanently delete a data point by its identifier. Use when you need to remove a data point from the database after confirming the identifier. Note: This endpoint is deprecated but still functional. |
| `dataPoints.getDataPoint` | `affinda.api.dataPoints.getDataPoint` | `read` | Tool to retrieve details of a specific data point by its identifier. Use when you need to inspect a data point's configuration, type, or structure. Data points represent individual fields that can be extracted from documents (e.g., 'name', 'email', 'date'). This endpoint is deprecated but still functional for retrieving data point metadata. |
| `dataPoints.listDataPoints` | `affinda.api.dataPoints.listDataPoints` | `read` | Tool to retrieve all data points. Returns both custom data points and Affinda's off-the-shelf data points. Use when you need to list available data points for extractors. |
| `dataPoints.updateDataPoint` | `affinda.api.dataPoints.updateDataPoint` | `write` | Tool to update data of a data point by its identifier. Use when you need to modify a data point's name, description, slug, parent, or mapping data source. Note: This endpoint is deprecated but still functional. |
| `dataSources.createDataSource` | `affinda.api.dataSources.createDataSource` | `write` | Tool to create a custom mapping data source. Use when you need to set up a new data source for mapping AI-extracted values to your database records. |
| `dataSources.createDataSourceValue` | `affinda.api.dataSources.createDataSourceValue` | `write` | Tool to add a new value to a mapping data source. Use when you need to add entries to a data source for mapping or validation purposes. The created value can then be referenced in document extraction and mapping workflows. |
| `dataSources.deleteDataSource` | `affinda.api.dataSources.deleteDataSource` | `destructive` | Permanently delete a mapping data source from the database by its identifier. This is a destructive operation that cannot be undone. Use when you need to remove a data source that is no longer needed or was created in error. |
| `dataSources.deleteDataSourceValue` | `affinda.api.dataSources.deleteDataSourceValue` | `destructive` | Tool to delete a specific value from a mapping data source. Use when you need to remove an incorrect or outdated value from a data source mapping. |
| `dataSources.getDataSource` | `affinda.api.dataSources.getDataSource` | `read` | Tool to retrieve details of a specific mapping data source by its identifier. Use when you need to fetch mapping data source metadata and schema configuration. |
| `dataSources.getDataSourceValue` | `affinda.api.dataSources.getDataSourceValue` | `read` | Tool to retrieve a specific value from a mapping data source. Use when you need to get details about a particular value in a data source. |
| `dataSources.getDataSourceValues` | `affinda.api.dataSources.getDataSourceValues` | `read` | Tool to retrieve all values from a mapping data source. Use when you need to list or search values in a specific data source after authentication. |
| `dataSources.listDataSources` | `affinda.api.dataSources.listDataSources` | `read` | Tool to retrieve the list of all custom mapping data sources. Use when you need to list or search for mapping data sources in your Affinda workspace. |
| `dataSources.replaceDataSourceValues` | `affinda.api.dataSources.replaceDataSourceValues` | `write` | Tool to completely replace all values in a mapping data source. Use when you need to bulk update or refresh an entire data source's value list. Note: For large data sources (>1000 values), new values may take a few minutes to become searchable after the operation completes. |
| `dataSources.updateDataSourceValue` | `affinda.api.dataSources.updateDataSourceValue` | `write` | Tool to update an existing value in a mapping data source. Use when you need to modify the label or description of an existing data source value. The value identifier itself cannot be changed. |
| `documents.addTagToDocuments` | `affinda.api.documents.addTagToDocuments` | `write` | Tool to add a tag to multiple documents in a single operation. Use when you need to organize or categorize multiple documents by assigning them a shared tag. Tags enable efficient filtering and grouping of documents in your workspace. |
| `documents.createDocument` | `affinda.api.documents.createDocument` | `write` | Upload a document to Affinda for parsing and data extraction. Use this action when you need to: - Parse resumes/CVs to extract candidate information - Process invoices to extract line items, amounts, and vendor details - Extract data from any supported document type (PDF, DOCX, images) You can upload either a file directly or provide a publicly accessible URL. The document will be processed by Affinda's AI extraction engine. Prerequisites: - For collection-based uploads: Get collection ID from get_collections action - For workspace-specific uploads: Get workspace ID from get_workspaces action |
| `documents.createFromDataDocuments` | `affinda.api.documents.createFromDataDocuments` | `write` | Create a document from structured resume or job description data for Search & Match. Use this when you have structured data to create a document programmatically without file uploads. Returns an identifier for subsequent status checks via /documents/{identifier}. |
| `documents.deleteDocument` | `affinda.api.documents.deleteDocument` | `destructive` | Tool to delete a specific document by its ID. Use when you need to remove a document that is incorrect or no longer needed, after confirming the document ID. |
| `documents.getDocument` | `affinda.api.documents.getDocument` | `read` | Retrieve full details and parsed data for a specific document by its identifier. Use this tool to get comprehensive information about a document including: - Processing status (ready/failed) - Document metadata (file name, page count, language) - Parsed/extracted data results - URLs for PDF, original file, and review page - Collection and workspace information The document identifier can be either a system-generated ID or a custom identifier set during document upload. |
| `documents.getDocumentRedacted` | `affinda.api.documents.getDocumentRedacted` | `read` | Tool to retrieve the redacted version of a document as a PDF file. Use when you need to get a document with sensitive information removed. The original document is not modified by this operation. |
| `documents.getDocuments` | `affinda.api.documents.getDocuments` | `read` | Tool to retrieve a list of all documents. Use when you need to list documents in your Affinda workspace after authentication. |
| `documents.removeTagFromDocuments` | `affinda.api.documents.removeTagFromDocuments` | `write` | Remove a tag from multiple documents in a single batch operation. Use this action to efficiently untag many documents at once rather than making individual remove requests. The operation is idempotent - removing a tag that isn't present on a document will not cause an error. |
| `documents.updateDocument` | `affinda.api.documents.updateDocument` | `write` | Tool to update specific fields of a document. Use when you need to rename, reassign, or adjust expiry and storage settings after document creation. |
| `documents.updateDocumentData` | `affinda.api.documents.updateDocumentData` | `write` | Update parsed data for a resume or job description document in Affinda. This endpoint is ONLY applicable for documents that have been processed with 'resume' or 'job-description' extractors. For other document types, use the batch_update_annotations endpoint instead. Use this tool when you need to correct or add parsed fields to an existing resume or job description document after it has been uploaded and processed. |
| `documentSplitters.getAllDocumentSplitters` | `affinda.api.documentSplitters.getAllDocumentSplitters` | `read` | Tool to get a list of all document splitters. Use when you need to retrieve document splitters, optionally filtered by organization. |
| `documentSplitters.getDocumentSplitter` | `affinda.api.documentSplitters.getDocumentSplitter` | `read` | Tool to retrieve details of a specific document splitter by its identifier. Use when you need to fetch metadata about a document splitter configuration. |
| `documentTypes.createDocumentType` | `affinda.api.documentTypes.createDocumentType` | `write` | Tool to create a new document type in the specified organization. Use when you need to define a new category of documents for processing. Document types allow you to organize and categorize documents within an organization. |
| `documentTypes.deleteDocumentType` | `affinda.api.documentTypes.deleteDocumentType` | `destructive` | Tool to permanently delete a document type by its identifier. Use when you need to remove a document type that is no longer needed. This is a destructive operation that cannot be undone. |
| `documentTypes.getDocumentType` | `affinda.api.documentTypes.getDocumentType` | `read` | Tool to retrieve details of a specific document type by its ID. Use when you need to confirm the configuration or metadata of a document type before processing documents. |
| `documentTypes.getDocumentTypeJsonSchema` | `affinda.api.documentTypes.getDocumentTypeJsonSchema` | `read` | Tool to generate a JSON schema from a document type by its identifier. Use when you need the structured schema definition for a document type to understand its field structure and validation rules. |
| `documentTypes.getDocumentTypePydanticModels` | `affinda.api.documentTypes.getDocumentTypePydanticModels` | `read` | Tool to generate Pydantic model code from a document type's schema. Use when you need type-safe Python models for parsing documents of a specific type. |
| `documentTypes.getDocumentTypes` | `affinda.api.documentTypes.getDocumentTypes` | `read` | Retrieve all document types accessible to the authenticated user. Returns a list containing each document type's identifier, name, description, ingest email, and organization. Use this to discover available document types before uploading or processing documents. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `documentTypes.updateDocumentType` | `affinda.api.documentTypes.updateDocumentType` | `write` | Tool to update a document type by its identifier. Use when you need to modify the name or description of an existing document type. |
| `extractors.createExtractor` | `affinda.api.extractors.createExtractor` | `write` | Tool to create a new extractor. Use when you need to define a custom document parser with specific fields and extraction rules, typically derived from a base extractor. |
| `extractors.deleteExtractor` | `affinda.api.extractors.deleteExtractor` | `destructive` | Permanently delete an extractor from Affinda by its identifier. This is a destructive operation that cannot be undone. Use Get Extractors first to verify the extractor ID before deletion. Note: This action is deprecated by the API. |
| `extractors.getExtractor` | `affinda.api.extractors.getExtractor` | `read` | Tool to retrieve detailed information about a specific extractor by its identifier. Use when you need complete metadata about an extractor's configuration, fields, and capabilities. |
| `extractors.getExtractors` | `affinda.api.extractors.getExtractors` | `read` | Retrieve all extractors available for an organization. Extractors are document parsing configurations that define what data to extract from documents. This includes both Affinda's built-in extractors (resumes, invoices, etc.) and any custom extractors created for the organization. Use this action to: - List available extractors before creating a collection - Find the identifier of a specific extractor type - Check if custom extractors exist for the organization |
| `extractors.updateExtractor` | `affinda.api.extractors.updateExtractor` | `write` | Tool to update specific fields of an extractor. Use when you need to modify an extractor's name, category, validation settings, or field groups after creation. |
| `indexes.createIndex` | `affinda.api.indexes.createIndex` | `write` | Tool to create a new index for search and match functionality. Use when you need to set up a new index for organizing and searching documents like resumes or job descriptions. |
| `indexes.deleteIndex` | `affinda.api.indexes.deleteIndex` | `destructive` | Tool to permanently delete an index from Affinda by its name. Use when you need to remove an index that is no longer needed. This is a destructive operation that cannot be undone. |
| `indexes.getIndexDocuments` | `affinda.api.indexes.getIndexDocuments` | `read` | Tool to retrieve all indexed documents for a specific index. Use when you need to list documents in a search index after creating or accessing an index. |
| `indexes.listIndexes` | `affinda.api.indexes.listIndexes` | `read` | Tool to retrieve a list of all search indexes. Use when you need to view available indexes for searching and matching documents in Affinda. |
| `indexes.updateIndex` | `affinda.api.indexes.updateIndex` | `write` | Tool to update the name of an existing search index. Use when you need to rename an index after creation. |
| `invitations.createInvitation` | `affinda.api.invitations.createInvitation` | `write` | Tool to create a new organization invitation. Use when you need to invite a user to join an organization with a specific role. |
| `invitations.deleteInvitation` | `affinda.api.invitations.deleteInvitation` | `destructive` | Tool to delete an invitation by its identifier. Use when you need to remove or revoke an invitation after confirming the invitation identifier. |
| `invitations.getAllInvitations` | `affinda.api.invitations.getAllInvitations` | `read` | Tool to retrieve all invitations you created or sent to you. Use when you need to list organization invitations with optional filtering by status, role, or organization. |
| `invitations.getInvitation` | `affinda.api.invitations.getInvitation` | `read` | Tool to retrieve details of a specific organization invitation by its identifier. Use when you need to check invitation status or get invitation metadata. |
| `invitations.updateInvitation` | `affinda.api.invitations.updateInvitation` | `write` | Tool to update an organization invitation's role. Use when you need to change the role assigned to an invitation before it is accepted. |
| `jobDescriptionSearch.createJobDescriptionSearch` | `affinda.api.jobDescriptionSearch.createJobDescriptionSearch` | `write` | Search through parsed job descriptions using custom criteria or resume matching. Use this action when you need to find job postings that match specific requirements such as skills, experience, location, education, or to find jobs matching a candidate's resume. Returns ranked results with detailed match scores. |
| `jobDescriptionSearch.createJobDescriptionSearchEmbedUrl` | `affinda.api.jobDescriptionSearch.createJobDescriptionSearchEmbedUrl` | `write` | Tool to create and return a signed URL for the embeddable job description search tool. Use this when you need to generate a URL that can be embedded on a web page to provide job description search functionality. Optionally pass config_override to customize display settings, field weights, and theme. |
| `jobDescriptionSearch.getJobDescriptionSearchConfig` | `affinda.api.jobDescriptionSearch.getJobDescriptionSearchConfig` | `read` | Tool to get the configuration for the logged in user's embeddable job description search tool. Returns display settings, field weights, and maximum results limits. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `jobDescriptionSearch.updateJobDescriptionSearchConfig` | `affinda.api.jobDescriptionSearch.updateJobDescriptionSearchConfig` | `write` | Tool to update the configuration for the logged in user's embeddable job description search tool. Use to configure display fields, field weights, maximum results, and UI customization options. |
| `mappings.createMapping` | `affinda.api.mappings.createMapping` | `write` | Tool to create a custom mapping for a data source. Use when you need to configure specific lookup settings for a MappingDataSource. Note: This endpoint is deprecated. |
| `mappings.deleteMapping` | `affinda.api.mappings.deleteMapping` | `destructive` | Tool to delete a specific mapping by its identifier. Use when you need to remove an unused or incorrect mapping from the database. Note: This endpoint is deprecated. |
| `mappings.getMapping` | `affinda.api.mappings.getMapping` | `read` | Tool to retrieve a specific mapping by its identifier. Use when you need to fetch mapping configuration details for a lookup against a MappingDataSource. |
| `mappings.listMappings` | `affinda.api.mappings.listMappings` | `read` | Tool to retrieve the list of all custom data mappings. Use when you need to list mappings for a specific data source. |
| `mappings.updateMapping` | `affinda.api.mappings.updateMapping` | `write` | Tool to update a specific mapping's settings. Use when you need to adjust score cutoff or ordering for a lookup against a MappingDataSource. |
| `occupationGroups.listOccupationGroups` | `affinda.api.occupationGroups.listOccupationGroups` | `read` | Tool to retrieve the list of searchable occupation groups. Use when you need to get available occupation categories for job matching or classification. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `organizationMemberships.getAllOrganizationMemberships` | `affinda.api.organizationMemberships.getAllOrganizationMemberships` | `read` | Retrieve all organization memberships across the account. Returns paginated results showing which users belong to which organizations. Use to audit organization access or find specific user memberships. |
| `organizationMemberships.getOrganizationMembership` | `affinda.api.organizationMemberships.getOrganizationMembership` | `read` | Tool to retrieve details of a specific organization membership by its ID. Use when you need to confirm a user's role and details within an organization. |
| `organizationMemberships.updateOrganizationMembership` | `affinda.api.organizationMemberships.updateOrganizationMembership` | `write` | Tool to update an organization membership's role. Use when you need to change a user's role within an organization. |
| `organizations.createOrganization` | `affinda.api.organizations.createOrganization` | `write` | Tool to create a new organization. Use when you have a unique organization name and want to group resources under it. |
| `organizations.deleteOrganization` | `affinda.api.organizations.deleteOrganization` | `destructive` | Permanently deletes an organization from Affinda. This action is destructive and cannot be undone. All workspaces, collections, and documents associated with the organization will also be deleted. Use the get_organizations action first to confirm the organization identifier before deletion. |
| `organizations.getOrganization` | `affinda.api.organizations.getOrganization` | `read` | Tool to retrieve details of a specific organization by its ID. Use when you need to fetch an organization's metadata after confirming its identifier. |
| `organizations.getOrganizations` | `affinda.api.organizations.getOrganizations` | `read` | Retrieves all organizations accessible to the authenticated user. Returns a list of organizations with details including identifier, name, user role, trial status, and configuration settings. Use the 'identifier' field from the response when calling other organization-related endpoints. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `organizations.updateOrganization` | `affinda.api.organizations.updateOrganization` | `write` | Tool to update specific fields of an organization. Use when you need to modify an organization's name, avatar, or webhook and validation settings after creation. |
| `resthooks.createResthookSubscription` | `affinda.api.resthooks.createResthookSubscription` | `write` | Tool to create a new RESTHook subscription. Use after confirming your webhook endpoint is ready to receive document event notifications. |
| `resthooks.deleteResthookSubscription` | `affinda.api.resthooks.deleteResthookSubscription` | `destructive` | Tool to delete a specific resthook subscription by ID. Use after confirming the subscription identifier when the webhook is no longer needed. |
| `resthooks.getResthookSubscription` | `affinda.api.resthooks.getResthookSubscription` | `read` | Tool to retrieve details of a specific resthook subscription by its ID. Use after confirming its creation to verify its settings. |
| `resthooks.getResthookSubscriptions` | `affinda.api.resthooks.getResthookSubscriptions` | `read` | Tool to retrieve a list of all RESTHook subscriptions. Use after authenticating to list all webhook subscriptions associated with your account. |
| `resthooks.updateResthookSubscription` | `affinda.api.resthooks.updateResthookSubscription` | `write` | Tool to update an existing RESTHook subscription. Use after confirming the subscription identifier to modify its URL, event, or activation status. |
| `resumeSearch.createResumeSearch` | `affinda.api.resumeSearch.createResumeSearch` | `write` | Tool to search through parsed resumes using three methods: match to a job description, match to a resume, or custom criteria. Use when you need to find candidates matching specific requirements. |
| `resumeSearch.createResumeSearchEmbedUrl` | `affinda.api.resumeSearch.createResumeSearchEmbedUrl` | `write` | Tool to create and return a signed URL for the embeddable resume search tool. Use this when you need to generate a URL that can be embedded on a web page to provide resume search functionality. Optionally pass config_override to customize display settings, field weights, and theme. |
| `resumeSearch.listResumeSearchConfig` | `affinda.api.resumeSearch.listResumeSearchConfig` | `read` | Tool to get the configuration for the logged in user's embeddable resume search tool. Use this to retrieve display field settings, search weights, maximum results, and theme customization options for the resume search interface. #### Output \| Parameter \| Type \| Required \| Description \| \|-----------\|------\|----------\|-------------\| \| `data` \| string \| Yes \| Data from the action execution \| \| `error` \| string \| No \| Error if any occurred during the execution of the action \| \| `successful` \| boolean \| Yes \| Whether or not the action execution was successful or not \| |
| `resumeSearch.listResumeSearchJobTitleSuggestions` | `affinda.api.resumeSearch.listResumeSearchJobTitleSuggestions` | `read` | Tool to get job title suggestions based on provided job title(s). Use when you need to find related job titles for resume search. |
| `resumeSearch.listResumeSearchSkillSuggestions` | `affinda.api.resumeSearch.listResumeSearchSkillSuggestions` | `read` | Tool to get skill suggestions based on provided skills. Use when you need to find related skills to expand or refine resume search queries. |
| `resumeSearch.updateResumeSearchConfig` | `affinda.api.resumeSearch.updateResumeSearchConfig` | `write` | Tool to update the configuration for the logged in user's embeddable resume search tool. Use this to modify which fields are displayed, their search weights, maximum results limit, distance units, custom actions, and theme settings. |
| `tags.createTag` | `affinda.api.tags.createTag` | `write` | Creates a new tag in the specified workspace. Tags can be used to categorize and label documents for organization and filtering. Each tag name must be unique within its workspace. Returns the created tag with its ID and metadata. |
| `tags.deleteTag` | `affinda.api.tags.deleteTag` | `destructive` | Permanently delete a tag from Affinda by its ID. This is a destructive operation that cannot be undone. Use Get Tags first to verify the tag ID before deletion. |
| `tags.getAllTags` | `affinda.api.tags.getAllTags` | `read` | Tool to list all tags. Use after confirming authentication to fetch tags across workspaces. |
| `tags.getTag` | `affinda.api.tags.getTag` | `read` | Tool to retrieve details of a specific tag by its ID. Use when you need to fetch information about a particular tag. |
| `tags.updateTag` | `affinda.api.tags.updateTag` | `write` | Tool to update data of a tag. Use when you need to modify a tag's name or reassign it to a different workspace. Only provide the fields you want to change; other fields will remain unchanged. |
| `validation.splitDocumentPages` | `affinda.api.validation.splitDocumentPages` | `write` | Split a document into multiple documents by dividing its pages. Use when you need to separate a multi-page document into distinct documents based on page groupings. Prerequisites: - Obtain the document identifier from create_document or get_document actions - Use get_document to retrieve page IDs for the pages you want to split Important notes: - Each page can only be assigned to one split - All pages should be accounted for across all splits - One split can optionally use the original document identifier to maintain the root document |
| `validationResults.createValidationResult` | `affinda.api.validationResults.createValidationResult` | `write` | Create a validation result for document annotations in Affinda. Use this tool to record validation outcomes after parsing a document. Validation results track whether specific annotations (extracted data fields) pass or fail business rules. Prerequisites: 1. Get a document identifier from Get Documents or Create Document 2. Get annotation IDs from Get Annotations for that document 3. Define a kebab-case rule slug that identifies your validation rule Example use cases: - Recording that an invoice total matches the sum of line items - Marking that a required field is present and valid - Logging validation failures for review workflows |
| `validationResults.createValidationResultsBatch` | `affinda.api.validationResults.createValidationResultsBatch` | `write` | Batch create multiple validation results for document annotations in a single API call. Use this action to efficiently record validation outcomes for multiple documents or rules at once, rather than making individual create requests for each validation result. This is particularly useful for bulk validation workflows or when validating multiple rules across many documents. Prerequisites: - Obtain document identifiers from Get Documents or Create Document actions - Get annotation IDs from Get Annotations for those documents - Define kebab-case rule slugs that identify your validation rules Common use cases: - Bulk recording of validation results across multiple documents - Validating multiple business rules on the same document - Automated validation workflows for large document batches |
| `validationResults.deleteValidationResults` | `affinda.api.validationResults.deleteValidationResults` | `destructive` | Delete multiple validation results in a single API call. Use this action to efficiently remove multiple validation results at once rather than making individual delete requests for each validation result. Prerequisites: - Obtain validation result IDs using the 'Get All Validation Results' action Common use cases: - Removing obsolete validation results in bulk - Cleaning up validation results after reprocessing documents - Deleting validation results for specific documents programmatically |
| `validationResults.getAllValidationResults` | `affinda.api.validationResults.getAllValidationResults` | `read` | Tool to list validation results for documents. Use after processing documents to inspect validation outcomes. |
| `workspaceMemberships.createWorkspaceMembership` | `affinda.api.workspaceMemberships.createWorkspaceMembership` | `write` | Tool to add a user to a workspace by creating a membership. Use when you need to grant a user access to a specific workspace. |
| `workspaceMemberships.deleteWorkspaceMembership` | `affinda.api.workspaceMemberships.deleteWorkspaceMembership` | `destructive` | Tool to remove a user from a workspace by membership ID. Use after confirming the workspace membership ID to revoke access. |
| `workspaceMemberships.getAllWorkspaceMemberships` | `affinda.api.workspaceMemberships.getAllWorkspaceMemberships` | `read` | Retrieve all workspace memberships across the account. Returns paginated results showing which users belong to which workspaces. Use to audit workspace access or find specific user memberships. |
| `workspaceMemberships.getWorkspaceMembership` | `affinda.api.workspaceMemberships.getWorkspaceMembership` | `read` | Tool to retrieve details of a specific workspace membership by its ID. Use when you need to confirm a user's role and details within a workspace after obtaining the membership identifier. |
| `workspaces.createWorkspace` | `affinda.api.workspaces.createWorkspace` | `write` | Tool to create a new workspace. Use when you need to programmatically create a workspace container within an organization. |
| `workspaces.deleteWorkspace` | `affinda.api.workspaces.deleteWorkspace` | `destructive` | Tool to delete a specific workspace by its ID. Use when you need to remove an unused workspace after confirming the workspace ID. |
| `workspaces.getUsageByWorkspace` | `affinda.api.workspaces.getUsageByWorkspace` | `read` | Retrieves monthly document processing usage statistics for a specific workspace. Returns a list of usage records showing how many documents were processed each month. Use this to track workspace activity, monitor consumption trends, or generate usage reports. |
| `workspaces.getWorkspace` | `affinda.api.workspaces.getWorkspace` | `read` | Tool to retrieve details of a specific workspace by its ID. Use when you need full workspace metadata after confirming its identifier. |
| `workspaces.getWorkspaces` | `affinda.api.workspaces.getWorkspaces` | `read` | Tool to retrieve a list of all workspaces. Use when you need to list workspaces filtered by organization and optional name. |
| `workspaces.updateWorkspace` | `affinda.api.workspaces.updateWorkspace` | `write` | Tool to update specific fields of a workspace. Use when you need to modify a workspace's name, visibility, document rejection policy, or other settings. Only provide the fields you want to change; other fields will remain unchanged. |

## Auth

Auth: API key. Corsair prompts your tenant for credentials on first use.

## Webhooks

No webhooks.

## Reference

Full docs, types, and examples: https://docs.corsair.dev/plugins/affinda

## License

Apache-2.0
