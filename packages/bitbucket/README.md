# @corsair-dev/bitbucket

Bitbucket Cloud OAuth 2.0 plugin for Corsair.

## Install

```bash
pnpm add @corsair-dev/bitbucket
```

## Endpoints

| Operation | Operation ID | Risk | Description |
|-----------|--------------|------|-------------|
| `commitsAndInsights.createRepositoriesCommitReportsAnnotations` | `bitbucket.api.commitsAndInsights.createRepositoriesCommitReportsAnnotations` | `write` | Adds multiple annotations to a commit report in bulk. Use when you need to add code analysis findings (vulnerabilities, code smells, bugs) to a report attached to a specific commit. |
| `commitsAndInsights.createRepositoriesCommits2` | `bitbucket.api.commitsAndInsights.createRepositoriesCommits2` | `write` | Tool to list commits from a revision using POST method. Identical to GET endpoint but allows sending include/exclude parameters in request body to avoid URL length limits. Use when include/exclude parameters are too long for query strings. |
| `commitsAndInsights.deleteCommitComment` | `bitbucket.api.commitsAndInsights.deleteCommitComment` | `destructive` | Permanently deletes a specific comment on a commit. Use when removing outdated, incorrect, or unwanted feedback on a commit. |
| `commitsAndInsights.deleteRepositoriesCommitReportsAnnotations` | `bitbucket.api.commitsAndInsights.deleteRepositoriesCommitReportsAnnotations` | `destructive` | Deletes a single annotation matching the provided ID from a commit report. Use when you need to remove a specific annotation from a code analysis report. |
| `commitsAndInsights.getCommitBuildStatus` | `bitbucket.api.commitsAndInsights.getCommitBuildStatus` | `read` | Get a specific build status for a commit in Bitbucket. Use when you need to check the status of a particular build/CI run for a commit. |
| `commitsAndInsights.getCommitChanges` | `bitbucket.api.commitsAndInsights.getCommitChanges` | `read` | Tool to retrieve a page of changes made in a specified commit, showing all changed files with their change statistics (lines added/removed, status). Use when you need to enumerate files modified in a specific commit or commit range. |
| `commitsAndInsights.getCommitComment` | `bitbucket.api.commitsAndInsights.getCommitComment` | `read` | Retrieves a specific comment from a commit by its ID. Use when you need to fetch details of a particular commit comment including content, author, timestamps, and inline location. |
| `commitsAndInsights.getCommitDiff` | `bitbucket.api.commitsAndInsights.getCommitDiff` | `read` | Tool to retrieve the unified diff between two provided revisions or for a single commit in a Bitbucket repository. Use when you need to see the actual code changes in a commit or between two commits. Supports filtering by file path and various diff options. |
| `commitsAndInsights.getRepositoriesCommit` | `bitbucket.api.commitsAndInsights.getRepositoriesCommit` | `read` | Tool to retrieve detailed information about a specific commit in a Bitbucket repository. Use when you need to get complete commit details including author, message, date, parents, and related links. |
| `commitsAndInsights.getRepositoriesCommitComments` | `bitbucket.api.commitsAndInsights.getRepositoriesCommitComments` | `read` | Retrieves all comments on a specific commit in a Bitbucket repository. Returns both global and inline code comments. Use when you need to view feedback, discussions, or notes left on a specific commit. |
| `commitsAndInsights.getRepositoriesCommitReport` | `bitbucket.api.commitsAndInsights.getRepositoriesCommitReport` | `read` | Returns a single report matching the provided ID from a commit. Use when you need to retrieve details of a specific analysis report (e.g., security scan, code coverage, test results, or bug report) for a commit. |
| `commitsAndInsights.getRepositoriesCommitReports` | `bitbucket.api.commitsAndInsights.getRepositoriesCommitReports` | `read` | Tool to get reports linked to a specific commit. Use when you need to retrieve analysis results, test reports, security scans, or code coverage data associated with a commit. |
| `commitsAndInsights.getRepositoriesCommitReportsAnnotations` | `bitbucket.api.commitsAndInsights.getRepositoriesCommitReportsAnnotations` | `read` | Returns a single annotation matching the provided ID from a commit report. Use when you need to retrieve details of a specific code analysis finding (e.g., vulnerability, code smell, or bug) identified in a commit. |
| `commitsAndInsights.getRepositoriesCommitStatuses` | `bitbucket.api.commitsAndInsights.getRepositoriesCommitStatuses` | `read` | Returns all build statuses (e.g., CI/CD pipeline results) for a specific commit. Use when you need to check build status, verify test results, or monitor deployment pipelines for a particular commit. |
| `commitsAndInsights.getRepositoriesMergeBase` | `bitbucket.api.commitsAndInsights.getRepositoriesMergeBase` | `read` | Get the merge base (best common ancestor) between two commits in a Bitbucket repository. Use when you need to find the common ancestor commit between two branches or commits for comparison or merge operations. |
| `commitsAndInsights.getRepositoryPatch` | `bitbucket.api.commitsAndInsights.getRepositoryPatch` | `read` | Tool to retrieve the git patch content for a Bitbucket repository at a specified revision or commit range. Use when you need to review code changes, generate diffs, or analyze modifications between commits. Returns raw patch in unified diff format. |
| `commitsAndInsights.listCommits` | `bitbucket.api.commitsAndInsights.listCommits` | `read` | Tool to retrieve a page of commits from a Bitbucket repository. Returns commits in reverse chronological order (newest first), similar to git log. Use when you need to browse commit history, filter commits by branch/tag, or restrict to commits affecting a specific path. |
| `commitsAndInsights.listCommitsFromRevision` | `bitbucket.api.commitsAndInsights.listCommitsFromRevision` | `read` | Tool to list commits starting from a specific revision in a Bitbucket repository. Commits are paginated and returned in reverse chronological order. Use when you need to retrieve commit history from a specific commit, branch, or tag. |
| `commitsAndInsights.listCommitsOnMaster` | `bitbucket.api.commitsAndInsights.listCommitsOnMaster` | `read` | Lists commits on the master branch of a Bitbucket repository. Use when you need to retrieve the commit history for the master branch, including commit messages, authors, dates, and parent relationships. |
| `commitsAndInsights.updateInsightsProjectsReposCommitsReports` | `bitbucket.api.commitsAndInsights.updateInsightsProjectsReposCommitsReports` | `write` | Create or update an insight report for a commit. Creates a new report if it doesn't exist, or replaces the existing one if a report already exists for the given repository, commit, and report key. Note: replacing an existing report will be rejected if the authenticated user was not the creator of the specified report. |
| `commitsAndInsights.updateRepositoriesCommitComments` | `bitbucket.api.commitsAndInsights.updateRepositoriesCommitComments` | `write` | Updates the contents of a comment on a commit. Use when you need to modify an existing comment's text on a commit. |
| `commitsAndInsights.updateRepositoriesCommitReportsAnnotations` | `bitbucket.api.commitsAndInsights.updateRepositoriesCommitReportsAnnotations` | `write` | Creates or updates an individual annotation for a commit report. Use when you need to add or modify code analysis findings (vulnerabilities, code smells, or bugs) identified in a commit. |
| `issues.createIssue` | `bitbucket.api.issues.createIssue` | `write` | Creates a new issue in a Bitbucket repository, setting the authenticated user as reporter; ensures assignee (if provided) has repository access, and that any specified milestone, version, or component IDs exist. |
| `issues.createIssueComment` | `bitbucket.api.issues.createIssueComment` | `write` | Adds a new comment with markdown support to an existing Bitbucket issue. |
| `issues.deleteIssue` | `bitbucket.api.issues.deleteIssue` | `destructive` | Permanently deletes a specific issue, identified by its `issue_id`, from the repository specified by `repo_slug` within the given `workspace`. |
| `issues.getRepositoriesIssuesVote` | `bitbucket.api.issues.getRepositoriesIssuesVote` | `read` | Tool to check whether the authenticated user has voted for a specific issue in a Bitbucket repository. Use when you need to verify if the current user has already voted on an issue before attempting to vote or unvote. |
| `issues.listIssues` | `bitbucket.api.issues.listIssues` | `read` | Lists issues in a Bitbucket repository with optional filtering by state, priority, kind, or assignee. Use when you need to discover issue IDs or get an overview of repository issues. |
| `issues.listVersions` | `bitbucket.api.issues.listVersions` | `read` | Lists versions (milestones) in a Bitbucket repository's issue tracker. Use when you need to discover available versions for associating with issues, or to retrieve version IDs for use with create_issue. |
| `issues.updateIssue` | `bitbucket.api.issues.updateIssue` | `write` | Updates an existing issue in a Bitbucket repository by modifying specified attributes; requires `workspace`, `repo_slug`, `issue_id`, and at least one attribute to update. |
| `pipelinesAndDeployments.createTeamsPipelinesConfigVariables` | `bitbucket.api.pipelinesAndDeployments.createTeamsPipelinesConfigVariables` | `write` | Creates a team-level pipeline configuration variable in Bitbucket. Use when you need to add environment variables or configuration values that should be available to all pipelines within a team. |
| `pipelinesAndDeployments.createUsersPipelinesConfigVariables` | `bitbucket.api.pipelinesAndDeployments.createUsersPipelinesConfigVariables` | `write` | Creates a user-level pipeline variable for Bitbucket pipelines. Use when you need to create account-level configuration variables that can be used across all repositories owned by the user. |
| `pipelinesAndDeployments.deleteUserPipelineVariable` | `bitbucket.api.pipelinesAndDeployments.deleteUserPipelineVariable` | `destructive` | Permanently deletes a user-level pipeline configuration variable identified by its UUID. Use this to remove pipeline variables that are no longer needed at the account level. |
| `pipelinesAndDeployments.getDeploymentEnvironmentVariables` | `bitbucket.api.pipelinesAndDeployments.getDeploymentEnvironmentVariables` | `read` | Retrieves deployment environment level variables for a specific Bitbucket repository environment. Use when you need to view or audit environment-specific configuration variables for deployments. |
| `pipelinesAndDeployments.getOpenidConfiguration` | `bitbucket.api.pipelinesAndDeployments.getOpenidConfiguration` | `read` | Retrieves the OpenID Connect discovery configuration for Bitbucket Pipelines OIDC. Use when integrating Bitbucket Pipelines with resource servers (AWS, GCP, Vault) using OpenID Connect authentication. Returns issuer URL, JWKS URI, and supported capabilities. |
| `pipelinesAndDeployments.getRepositoriesEnvironments2` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesEnvironments2` | `read` | Retrieve detailed information about a specific deployment environment in a Bitbucket repository. Use when you need to get environment configuration, deployment settings, or check environment properties like locks and restrictions. |
| `pipelinesAndDeployments.getRepositoriesPipelines2` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelines2` | `read` | Retrieve a specified pipeline from a Bitbucket repository. Use when you need to get detailed information about a specific pipeline execution including its status, build number, and results. |
| `pipelinesAndDeployments.getRepositoriesPipelinesConfigCaches` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesConfigCaches` | `read` | Retrieves the repository pipelines caches. Use when you need to list all caches configured for Bitbucket Pipelines in a specific repository. |
| `pipelinesAndDeployments.getRepositoriesPipelinesConfigRunners` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesConfigRunners` | `read` | Retrieves the list of self-hosted runners configured for a repository's pipelines. Use when you need to view available runners for pipeline execution. |
| `pipelinesAndDeployments.getRepositoriesPipelinesConfigSchedules` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesConfigSchedules` | `read` | Retrieves configured pipeline schedules for a Bitbucket repository. Use when you need to view scheduled pipeline runs and their cron patterns. |
| `pipelinesAndDeployments.getRepositoriesPipelinesConfigSshKnownHosts` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesConfigSshKnownHosts` | `read` | Retrieves repository-level SSH known hosts configured for Bitbucket Pipelines. Use when you need to list or verify SSH known hosts that Pipelines can connect to during builds. |
| `pipelinesAndDeployments.getRepositoriesPipelinesConfigVariables` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesConfigVariables` | `read` | Retrieves repository-level pipeline variables for a specific Bitbucket repository. Use when you need to view or audit pipeline configuration variables that are scoped to a repository. |
| `pipelinesAndDeployments.getRepositoriesPipelinesSteps` | `bitbucket.api.pipelinesAndDeployments.getRepositoriesPipelinesSteps` | `read` | Retrieves all steps for a given pipeline. Use when you need to inspect the individual steps of a pipeline execution, including their state, duration, and commands. |
| `pipelinesAndDeployments.listDeployments` | `bitbucket.api.pipelinesAndDeployments.listDeployments` | `read` | Lists deployments for a specified Bitbucket repository. Use when you need to view deployment history and status across environments. |
| `pipelinesAndDeployments.listPipelines` | `bitbucket.api.pipelinesAndDeployments.listPipelines` | `read` | Tool to find pipelines in a Bitbucket repository. Returns pipeline metadata including state, trigger, and duration. Use when you need to browse pipeline history or check pipeline status. |
| `pipelinesAndDeployments.listRepositoriesEnvironments` | `bitbucket.api.pipelinesAndDeployments.listRepositoriesEnvironments` | `read` | List all deployment environments configured for a Bitbucket repository. Use when you need to view available environments for deployments, check environment configurations, or select an environment for deployment operations. |
| `pipelinesAndDeployments.updateTeamsPipelinesConfigVariables` | `bitbucket.api.pipelinesAndDeployments.updateTeamsPipelinesConfigVariables` | `write` | Updates a team-level pipeline configuration variable in Bitbucket. Use when you need to modify existing environment variables or configuration values that are available to all pipelines within a team. |
| `pipelinesAndDeployments.updateUsersPipelinesConfigVariables` | `bitbucket.api.pipelinesAndDeployments.updateUsersPipelinesConfigVariables` | `write` | Updates a user-level pipeline variable for Bitbucket pipelines. Use when you need to modify account-level configuration variables such as changing the value, key name, or security status. |
| `pullRequests.approvePullRequest` | `bitbucket.api.pullRequests.approvePullRequest` | `write` | Tool to approve a pull request as the authenticated user. Use when you need to formally approve changes in a pull request review process. |
| `pullRequests.createPullRequest` | `bitbucket.api.pullRequests.createPullRequest` | `write` | Creates a new pull request in a specified Bitbucket repository, ensuring the source branch exists and is distinct from the (optional) destination branch. |
| `pullRequests.createPullRequestComment` | `bitbucket.api.pullRequests.createPullRequestComment` | `write` | Creates a new comment on a Bitbucket pull request. Supports top-level comments, threaded replies, and inline code comments. Use when providing feedback on a PR, replying to existing comments, or commenting on specific code lines. |
| `pullRequests.getPullRequest` | `bitbucket.api.pullRequests.getPullRequest` | `read` | Get a single pull request by ID with complete details. |
| `pullRequests.getPullRequestComment` | `bitbucket.api.pullRequests.getPullRequestComment` | `read` | Tool to retrieve a specific comment from a pull request by its ID. Use when you need to fetch details of a particular pull request comment including content, author, timestamps, and inline location. |
| `pullRequests.getPullRequestCommits` | `bitbucket.api.pullRequests.getPullRequestCommits` | `read` | Tool to retrieve commits for a specified pull request. Use when reviewing the commit history of a PR or analyzing changes included in a pull request. |
| `pullRequests.getPullRequestDiff` | `bitbucket.api.pullRequests.getPullRequestDiff` | `read` | Tool to fetch the unified diff for a Bitbucket pull request (follows 302 redirect to repository diff). Use when reviewing code changes in a PR. Supports optional truncation for large diffs via max_chars parameter. |
| `pullRequests.getPullRequestDiffstat` | `bitbucket.api.pullRequests.getPullRequestDiffstat` | `read` | Tool to get the diffstat for a Bitbucket pull request, showing all changed files with their change statistics (lines added/removed, status). Use when you need to enumerate files modified in a PR. |
| `pullRequests.getRepositoriesPullrequestsActivity` | `bitbucket.api.pullRequests.getRepositoriesPullrequestsActivity` | `read` | Get paginated activity log for all pull requests in a repository. Returns comments, updates, approvals, and request changes. Use when you need to track pull request activity history. |
| `pullRequests.getRepositoriesPullrequestsComments` | `bitbucket.api.pullRequests.getRepositoriesPullrequestsComments` | `read` | Retrieves a paginated list of comments on a specific pull request in a Bitbucket repository. Returns global, inline, and threaded comments. Use when you need to view feedback, discussions, or reviews left on a pull request. |
| `pullRequests.getRepositoriesPullrequestsStatuses` | `bitbucket.api.pullRequests.getRepositoriesPullrequestsStatuses` | `read` | Returns all build statuses (e.g., CI/CD pipeline results) for a specific pull request. Use when you need to check build status, verify test results, or monitor deployment pipelines for a pull request. |
| `pullRequests.listPullRequests` | `bitbucket.api.pullRequests.listPullRequests` | `read` | Lists pull requests in a specified, accessible Bitbucket repository, optionally filtering by state (OPEN, MERGED, DECLINED). |
| `pullRequests.listPullRequestTasks` | `bitbucket.api.pullRequests.listPullRequestTasks` | `read` | Lists all tasks associated with a pull request in a Bitbucket repository. Use when you need to view or track tasks on a PR. |
| `pullRequests.requestPullRequestChanges` | `bitbucket.api.pullRequests.requestPullRequestChanges` | `write` | Tool to request changes on a pull request as the authenticated user. Use when you need to formally request changes in a pull request review process, indicating the PR needs modifications before approval. |
| `repositories.createRepository` | `bitbucket.api.repositories.createRepository` | `write` | Creates a new Bitbucket 'git' repository in a specified workspace, defaulting to the workspace's oldest project if `project_key` is not provided. |
| `repositories.deleteRepository` | `bitbucket.api.repositories.deleteRepository` | `destructive` | Permanently deletes a specified Bitbucket repository; this action is irreversible and does not affect forks. |
| `repositories.getRepositoriesWatchers` | `bitbucket.api.repositories.getRepositoriesWatchers` | `read` | Retrieves a paginated list of all the watchers on the specified repository. Use when you need to see who is watching a particular repository. |
| `repositories.getRepository` | `bitbucket.api.repositories.getRepository` | `read` | Retrieves detailed information about a specific repository in a Bitbucket workspace. Use when you need to get repository metadata, settings, or details. |
| `repositories.listRepositories` | `bitbucket.api.repositories.listRepositories` | `read` | Retrieves a paginated list of all public repositories on Bitbucket. Use when you need to discover or search across public repositories, optionally filtered by role, query string, creation date, or sorted by various fields. |
| `repositories.listRepositoriesInWorkspace` | `bitbucket.api.repositories.listRepositoriesInWorkspace` | `read` | Lists repositories in a specified Bitbucket workspace, accessible to the authenticated user, with options to filter by role or query string, and sort results. Responses are paginated; iterate using the `next` field in each response until it is absent to retrieve all repositories. |
| `searchAndDiscovery.getHookEvents` | `bitbucket.api.searchAndDiscovery.getHookEvents` | `read` | Retrieves a paginated list of all valid webhook events for a specified entity type (repository or workspace). Use when you need to discover available webhook event types for subscription or webhook configuration. |
| `searchAndDiscovery.getWorkspacesSearchCode` | `bitbucket.api.searchAndDiscovery.getWorkspacesSearchCode` | `read` | Tool to search for code in the repositories of the specified workspace. Use when you need to find specific code patterns, function definitions, or text across all repositories in a workspace. |
| `searchAndDiscovery.searchTeamCode` | `bitbucket.api.searchAndDiscovery.searchTeamCode` | `read` | Search for code in repositories of a specified team. Matches can occur in file content or paths. Note: Teams endpoints were deprecated in Oct 2020; use workspace search endpoints for new integrations. |
| `searchAndDiscovery.searchUserRepositoriesCode` | `bitbucket.api.searchAndDiscovery.searchUserRepositoriesCode` | `read` | Tool to search for code in the repositories of a specified user. Use when you need to find specific code patterns, functions, or text across all repositories owned by a user. |
| `snippets.createSnippetComment` | `bitbucket.api.snippets.createSnippetComment` | `write` | Posts a new top-level comment or a threaded reply to an existing comment on a specified Bitbucket snippet. |
| `snippets.deleteSnippetsWatch` | `bitbucket.api.snippets.deleteSnippetsWatch` | `write` | Stops watching a specific snippet. Use when you want to unsubscribe from notifications for a snippet. |
| `snippets.getSnippet` | `bitbucket.api.snippets.getSnippet` | `read` | Retrieves a specific Bitbucket snippet by its encoded ID from an existing workspace, returning its metadata and file structure. |
| `snippets.getSnippetsWatch` | `bitbucket.api.snippets.getSnippetsWatch` | `read` | Checks if the current user is watching a specific snippet. Use when you need to verify watch status for a snippet. |
| `snippets.listSnippets` | `bitbucket.api.snippets.listSnippets` | `read` | Returns all snippets accessible to the authenticated user. Use when you need to discover or list snippets, optionally filtered by role (owner, contributor, or member). |
| `sourceAndRefs.browseRepositoryPath` | `bitbucket.api.sourceAndRefs.browseRepositoryPath` | `read` | Tool to retrieve content for a file path or browse directory contents at a specified revision in a Bitbucket repository. Use when you need flexible access to repository content - returns raw file data for files or paginated directory listings for directories. |
| `sourceAndRefs.createBranch` | `bitbucket.api.sourceAndRefs.createBranch` | `write` | Creates a new branch in a Bitbucket repository from a target commit hash; the branch name must be unique, adhere to Bitbucket's naming conventions, and not include the 'refs/heads/' prefix. |
| `sourceAndRefs.getBranch` | `bitbucket.api.sourceAndRefs.getBranch` | `read` | Retrieves detailed information about a specific branch in a Bitbucket repository. Use when you need to get branch metadata, the commit it points to, or verify a branch exists. |
| `sourceAndRefs.getFileFromRepository` | `bitbucket.api.sourceAndRefs.getFileFromRepository` | `read` | Retrieves a specific file's content from a Bitbucket repository at a given commit (hash, branch, or tag), failing if the file path is invalid for that commit. |
| `sourceAndRefs.getRawFileContent` | `bitbucket.api.sourceAndRefs.getRawFileContent` | `read` | Tool to retrieve the raw content of a file from a Bitbucket repository at a specified commit, branch, or tag. Use when you need to read file contents from a specific revision. |
| `sourceAndRefs.getRepositoriesBranchingModel` | `bitbucket.api.sourceAndRefs.getRepositoriesBranchingModel` | `read` | Return the branching model as applied to the repository. Use when you need to understand the repository's branch workflow configuration, including development/production branches and branch type prefixes. |
| `sourceAndRefs.getRepositoriesEffectiveBranchingModel` | `bitbucket.api.sourceAndRefs.getRepositoriesEffectiveBranchingModel` | `read` | Retrieves the effective branching model for a Bitbucket repository, showing which branching model is currently applied (including any inheritance from project-level settings). Use when you need to understand the repository's branch workflow configuration, including development and production branches and branch type prefixes. |
| `sourceAndRefs.getRepositoriesFilehistory` | `bitbucket.api.sourceAndRefs.getRepositoriesFilehistory` | `read` | Returns a paginated list of commits that modified the specified file. Use when you need to track file changes over time, find who modified a file, or determine when a file was created or last changed. |
| `sourceAndRefs.getRepositoriesRefs` | `bitbucket.api.sourceAndRefs.getRepositoriesRefs` | `read` | Returns the branches and tags in the repository. Use when you need to list all refs (both branches and tags) in a single API call with optional filtering by type, name pattern, or commit hash. |
| `sourceAndRefs.getRepositoriesRefsTags` | `bitbucket.api.sourceAndRefs.getRepositoriesRefsTags` | `read` | Retrieves detailed information about a specific tag in a Bitbucket repository. Use when you need to get tag metadata, target commit details, or verify a tag exists. |
| `sourceAndRefs.getRepositoriesSrc` | `bitbucket.api.sourceAndRefs.getRepositoriesSrc` | `read` | Lists the contents of the root directory on the repository's main branch without needing to specify a commit or branch. This endpoint redirects to the main branch automatically. |
| `sourceAndRefs.listBranches` | `bitbucket.api.sourceAndRefs.listBranches` | `read` | Lists branches in a Bitbucket repository with optional server-side filtering by name pattern (BBQL) and sorting. Use when you need to discover available branches, search for specific branch patterns, or navigate repository branch structure. |
| `sourceAndRefs.listRepositoryPaths` | `bitbucket.api.sourceAndRefs.listRepositoryPaths` | `read` | Lists file and directory entries under a repository path at a given revision, with optional breadth-first recursion via max_depth for repository traversal and scanning. Fails if the path points to a file rather than a directory. |
| `sourceAndRefs.listTags` | `bitbucket.api.sourceAndRefs.listTags` | `read` | Lists tags in a Bitbucket repository with optional server-side filtering by name pattern or commit hash (BBQL) and sorting. Use when you need to discover available tags, search for specific tag patterns, or find tags pointing to specific commits. |
| `usersAndPermissions.getCurrentUser2` | `bitbucket.api.usersAndPermissions.getCurrentUser2` | `read` | Tool to retrieve complete profile information for the currently authenticated Bitbucket user. Use when you need comprehensive user details including account_id, username, nickname, and other profile fields. |
| `usersAndPermissions.getSshLatestKeys` | `bitbucket.api.usersAndPermissions.getSshLatestKeys` | `read` | Retrieves a paginated list of SSH keys for a specified Bitbucket user. Use when you need to view or audit SSH keys configured for a user account. |
| `usersAndPermissions.getUser` | `bitbucket.api.usersAndPermissions.getUser` | `read` | Retrieves public profile information for a specific Bitbucket user by username or UUID. Use when you need to get user details like display name, avatar, creation date, and links to related resources. |
| `usersAndPermissions.getUserEmails` | `bitbucket.api.usersAndPermissions.getUserEmails` | `read` | Returns all the authenticated user's email addresses, both confirmed and unconfirmed. Use when you need to retrieve all email addresses associated with the current user's account. |
| `usersAndPermissions.getUserEmails2` | `bitbucket.api.usersAndPermissions.getUserEmails2` | `read` | Retrieves details about a specific email address for the authenticated user. Use when you need to check if an email is primary, confirmed, or verify email ownership. |
| `usersAndPermissions.getUserPermissionsRepositories` | `bitbucket.api.usersAndPermissions.getUserPermissionsRepositories` | `read` | Returns an object for each repository the caller has explicit access to, including their permission level. Use when you need to discover which repositories the authenticated user can access and their specific permissions. |
| `usersAndPermissions.getUserPermissionsWorkspaces` | `bitbucket.api.usersAndPermissions.getUserPermissionsWorkspaces` | `read` | Retrieves workspace memberships and permission levels for the authenticated user. Returns an object for each workspace the caller is a member of, along with their effective role (highest privilege level). Use when you need to determine which workspaces a user can access and their permission level in each. |
| `usersAndPermissions.getUserWorkspaces` | `bitbucket.api.usersAndPermissions.getUserWorkspaces` | `read` | Tool to retrieve all workspaces accessible to the authenticated user. Use when you need to list workspaces the current user can access, optionally filtered by workspace slug or permission level. |
| `workspacesAndProjects.getProjectsRepos` | `bitbucket.api.workspacesAndProjects.getProjectsRepos` | `read` | Retrieves repositories from a project in a Bitbucket workspace. Use when you need to list all repositories belonging to a specific project key within a workspace. |
| `workspacesAndProjects.getWorkspace` | `bitbucket.api.workspacesAndProjects.getWorkspace` | `read` | Retrieves detailed information about a specific Bitbucket workspace. Use when you need to get workspace metadata, settings, or details. |
| `workspacesAndProjects.getWorkspacesPullrequests` | `bitbucket.api.workspacesAndProjects.getWorkspacesPullrequests` | `read` | Tool to get all workspace pull requests authored by a specified user. Use when you need to retrieve pull requests created by a specific user across all repositories in a workspace. |
| `workspacesAndProjects.listWorkspaceMembers` | `bitbucket.api.workspacesAndProjects.listWorkspaceMembers` | `read` | Lists all members of a specified Bitbucket workspace; the workspace must exist. |
| `workspacesAndProjects.listWorkspaceProjects` | `bitbucket.api.workspacesAndProjects.listWorkspaceProjects` | `read` | Lists projects in a specified Bitbucket workspace. Use when you need to retrieve all projects belonging to a workspace. |
| `workspacesAndProjects.listWorkspaces` | `bitbucket.api.workspacesAndProjects.listWorkspaces` | `read` | Lists Bitbucket workspaces accessible to the authenticated user, optionally filtered and sorted. Results are paginated; follow the `next` field in each response to retrieve subsequent pages until `next` is absent. When multiple workspaces are returned, verify the correct `slug` or UUID before passing to downstream tools. |

## Auth

Auth: OAuth 2.0. Corsair prompts your tenant for credentials on first use.

## Webhooks

No webhooks.

## Reference

Full docs, types, and examples: https://docs.corsair.dev/plugins/bitbucket

## License

Apache-2.0
