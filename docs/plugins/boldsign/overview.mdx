---
title: Overview
description: "Boldsign plugin for Corsair"
---

Use **Boldsign** through Corsair: one client, typed API calls, local DB sync.

**What you get:**

- 14 typed API operations
- 3 synced entities (`brands`, `custom_fields`, `documents`) for fast `.search()` / `.list()`

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/boldsign
```
```bash yarn
yarn add corsair @corsair-dev/boldsign
```
```bash pnpm
pnpm install corsair @corsair-dev/boldsign
```
```bash bun
bun add corsair @corsair-dev/boldsign
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { boldsign } from '@corsair-dev/boldsign';

export const corsair = createCorsair({
	plugins: [
		boldsign(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="OAuth 2.0 (Recommended)">

Open [hub.corsair.dev](https://hub.corsair.dev/dashboard), navigate to your project, and enter the **client ID** and **client secret** from your Boldsign OAuth app.

```ts
boldsign()
```

More: [OAuth 2.0](/concepts/oauth)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'boldsign',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**`brands.get`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.boldsign.api.brands.get({});
```


**`customFields.create`**

```ts
const tenant = corsair.withTenant('acme');
await tenant.boldsign.api.customFields.create({});
```


See the full list on the [API](/plugins/boldsign/api) page.

## Query synced data

Synced entities support `tenant.boldsign.db.<entity>.search()` and `.list()`: `brands`, `custom_fields`, `documents`.

See [Database](/plugins/boldsign/database) for filters and operators.

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/boldsign/api">
    Every `boldsign.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/boldsign/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
</CardGroup>
