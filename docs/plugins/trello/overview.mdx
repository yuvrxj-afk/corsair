---
title: Overview
description: "Kanban board tool for organizing tasks, cards, lists, and lightweight project management."
---

Use **Trello** through Corsair: one client, typed API calls, local DB sync, and incoming webhooks.

**What you get:**

- 25 typed API operations
- 5 synced entities (`boards`, `cards`, `labels`, `lists`, `members`) for fast `.search()` / `.list()`
- 6 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
<CodeGroup>
```bash npm
npm install corsair @corsair-dev/trello
```
```bash yarn
yarn add corsair @corsair-dev/trello
```
```bash pnpm
pnpm install corsair @corsair-dev/trello
```
```bash bun
bun add corsair @corsair-dev/trello
```
</CodeGroup>

</Step>

<Step title="Add the plugin">
```ts corsair.ts
import Database from 'better-sqlite3';
import { createCorsair } from 'corsair';
import { trello } from '@corsair-dev/trello';

export const corsair = createCorsair({
	plugins: [
		trello(),
	],
	database: new Database('corsair.db'),
	kek: process.env.CORSAIR_KEK!,
	hub: {
		projectApiKey: process.env.CORSAIR_API_KEY!,
		signingSecret: process.env.CORSAIR_SIGNING_SECRET!,
	},
});
```

Multi-tenancy is the default — scope calls with `corsair.withTenant(id)`. See [Quick start](/quick-start) for KEK + Hub keys, and [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Step>

<Step title="Choose authentication">
<Tabs>
<Tab title="API Key (Recommended)">

No setup required yet. When you make your first request as a tenant, Corsair prompts for the API key.

Provider walkthrough: [Get Credentials](/plugins/trello/get-credentials).

```ts
trello()
```

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

</Step>

<Step title="Connect a tenant">
Mint a connect link and send the tenant to it. Hub hosts the page and delivers the result to your app — see [Connect / OAuth](/management/connect).

```ts
const { connectUrl } = await corsair.manage.connect.createLink({
	plugin: 'trello',
	tenantId: 'acme',
});
// redirect the user's browser to connectUrl
```

</Step>

</Steps>

## Example API calls

**List cards**

```ts
const tenant = corsair.withTenant('acme');
await tenant.trello.api.cards.list({ listId: '64a1b2c3d4e5f6a7b8c9d0e1' });
```


**Create a card**

```ts
const tenant = corsair.withTenant('acme');
await tenant.trello.api.cards.create({ name: 'Follow up from Corsair', idList: '64a1b2c3d4e5f6a7b8c9d0e1' });
```


See the full list on the [API](/plugins/trello/api) page.

## Query synced data

Search synced cards without hitting Trello.

```ts
const tenant = corsair.withTenant('acme');
const rows = await tenant.trello.db.cards.search({
	data: { closed: false },
	limit: 50,
});
```

Synced entities: `boards`, `cards`, `labels`, `lists`, `members`. See [Database](/plugins/trello/database) for filters and operators.

## Webhooks

Fires when a Trello card is created.

```ts
trello({
    webhookHooks: {
        cards: {
            cardCreated: {
                after: async (ctx, result) => {
                    console.log('Trello card created:', result.data?.action.data.card?.id);
                }
            },
        },
    },
})
```

Mount your framework handler once (see [Frameworks](/frameworks/next)), then point the provider at that URL. Full event list: [Webhooks](/plugins/trello/webhooks). Concepts: [Webhooks](/concepts/webhooks), [Hooks](/concepts/hooks).

## What's next

<CardGroup cols={2}>
  <Card title="API reference" href="/plugins/trello/api">
    Every `trello.api.*` operation with input and output types.
  </Card>
  <Card title="Database" href="/plugins/trello/database">
    Synced entities, search filters, and operators.
  </Card>
  <Card title="Webhooks" href="/plugins/trello/webhooks">
    Event paths, payloads, and `webhookHooks` examples.
  </Card>
  <Card title="Connect / OAuth" href="/management/connect">
    createLink, Hub delivery, and tenant connect flows.
  </Card>
  <Card title="Use with agents" href="/mcp-adapters/mcp-adapters">
    Expose this plugin's operations as MCP tools.
  </Card>
  <Card title="Get credentials" href="/plugins/trello/get-credentials">
    Provider-console walkthrough for keys and OAuth apps.
  </Card>
</CardGroup>
